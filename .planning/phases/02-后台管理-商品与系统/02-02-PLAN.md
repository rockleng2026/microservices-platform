# Phase 2 Plan 02: 分类与轮播图管理

**Phase:** 02-后台管理-商品与系统
**Plan:** 02-02
**Type:** execute
**Wave:** 2
**Depends On:** [02-01] (needs entity/mapper from foundation wave)
**Files Modified:**
- `zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminCategoryController.java` (NEW)
- `zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminBannerController.java` (NEW)
- `zlt-business/mall-center/src/main/java/com/central/mall/service/IAdminCategoryService.java` (NEW)
- `zlt-business/mall-center/src/main/java/com/central/mall/service/IAdminBannerService.java` (NEW)
- `zlt-business/mall-center/src/main/java/com/central/mall/service/impl/AdminCategoryServiceImpl.java` (NEW)
- `zlt-business/mall-center/src/main/java/com/central/mall/service/impl/AdminBannerServiceImpl.java` (NEW)
**Autonomous:** true
**Requirements:** GOODS-05, SYS-01

---

<objective>
Implement admin CRUD for product categories (tree structure) and banner carousel. Category management supports D-10 (增删改+排序). Banner management supports D-04 (混合模式：内部商品+外部URL).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@D:\code\microservices-platform\.planning\ROADMAP.md
@D:\code\microservices-platform\.planning\phases\02-后台管理-商品与系统\02-CONTEXT.md

## Implementation Decisions from 02-CONTEXT.md

| D-ID | Decision | Implementation |
|------|----------|----------------|
| D-04 | 轮播图链接混合模式 | linkType=1 means goods (goodsId field used), linkType=2 means external (externalUrl field used) |
| D-10 | 分类操作增删改+排序 | All CRUD + sort field for ordering |

## Phase 1 Pattern Reference (from GoodsController.java)

```java
@RestController
@RequestMapping("/api/mall/admin/category")
@RequiredArgsConstructor
@Tag(name = "管理员-分类管理", description = "后台分类管理接口")
public class AdminCategoryController {
    private final IAdminCategoryService adminCategoryService;

    @GetMapping("/list")
    @Operation(summary = "获取分类列表(树形)")
    public Result<List<AdminCategoryDTO>> getCategoryTree() { ... }

    @PostMapping
    @Operation(summary = "新增分类")
    public Result addCategory(@RequestBody AdminCategoryDTO dto) { ... }

    @PutMapping
    @Operation(summary = "编辑分类")
    public Result updateCategory(@RequestBody AdminCategoryDTO dto) { ... }

    @DeleteMapping("/{id}")
    @Operation(summary = "删除分类")
    public Result deleteCategory(@PathVariable Long id) { ... }
}
```

## Entity References

**MallCategory** (existing): id, tenantId, parentId, name, sort, icon, status, createTime, updateTime

**MallBanner** (from 02-01): id, tenantId, title, imageUrl, linkType (1=goods,2=external), goodsId, externalUrl, sort, status, createTime, updateTime
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IAdminCategoryService interface and AdminCategoryServiceImpl</name>
  <files>
    zlt-business/mall-center/src/main/java/com/central/mall/service/IAdminCategoryService.java
    zlt-business/mall-center/src/main/java/com/central/mall/service/impl/AdminCategoryServiceImpl.java
  </files>
  <action>
**IAdminCategoryService.java** methods:
- getCategoryTree(): List<AdminCategoryDTO> — returns full tree, including children
- addCategory(AdminCategoryDTO dto): boolean — create new category, validate parent exists if parentId > 0
- updateCategory(AdminCategoryDTO dto): boolean — update by id, preserve children
- deleteCategory(Long id): boolean — delete by id, recursively delete children if parent
- sortCategories(List<Long> ids): boolean — D-10: reorder by passing ordered list of ids

**AdminCategoryServiceImpl.java**:
- extends ServiceImpl<MallCategoryMapper, MallCategory>
- implements IAdminCategoryService
- Inject MallCategoryMapper via constructor (or @RequiredArgsConstructor)
- getCategoryTree() reuses existing tree-building logic pattern from GoodsServiceImpl
- addCategory/updateCategory: use LambdaUpdateWrapper or updateById
- deleteCategory: hard delete (no soft delete for categories), cascade children
- sortCategories: batch update sort field using LambdaUpdateWrapper

Use TenantInterceptor.getCurrentTenantId() for tenant_id on all operations.
</action>
  <verify>
    <automated>grep -c "class AdminCategoryServiceImpl" zlt-business/mall-center/src/main/java/com/central/mall/service/impl/AdminCategoryServiceImpl.java</automated>
  </verify>
  <done>AdminCategoryService with full CRUD + sort + tree methods implemented</done>
</task>

<task type="auto">
  <name>Task 2: Create AdminCategoryController</name>
  <files>zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminCategoryController.java</files>
  <action>
Create AdminCategoryController in controller/admin/ package:

Endpoints:
- GET /api/mall/admin/category/list — getCategoryTree()
- POST /api/mall/admin/category — addCategory(@RequestBody AdminCategoryDTO)
- PUT /api/mall/admin/category — updateCategory(@RequestBody AdminCategoryDTO)
- DELETE /api/mall/admin/category/{id} — deleteCategory(@PathVariable)
- PUT /api/mall/admin/category/sort — sortCategories(@RequestBody List<Long> ids)

Use @Tag(name = "管理员-分类管理"). All endpoints return Result. Validate input: name required, parentId must exist if > 0.
</action>
  <verify>
    <automated>grep -c "@RequestMapping.*admin/category" zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminCategoryController.java</automated>
  </verify>
  <done>AdminCategoryController with 5 endpoints for full CRUD + sort</done>
</task>

<task type="auto">
  <name>Task 3: Create IAdminBannerService interface and AdminBannerServiceImpl</name>
  <files>
    zlt-business/mall-center/src/main/java/com/central/mall/service/IAdminBannerService.java
    zlt-business/mall-center/src/main/java/com/central/mall/service/impl/AdminBannerServiceImpl.java
  </files>
  <action>
**IAdminBannerService.java** methods:
- getBannerList(): List<BannerDTO> — returns all banners ordered by sort ASC
- addBanner(BannerDTO dto): boolean — create, linkType validation (1=goods,2=external)
- updateBanner(BannerDTO dto): boolean — update by id
- deleteBanner(Long id): boolean — delete by id
- updateSort(Long id, Integer sort): boolean — update single banner sort

**AdminBannerServiceImpl.java**:
- extends ServiceImpl<MallBannerMapper, MallBanner>
- implements IAdminBannerService
- Inject MallBannerMapper
- getBannerList: selectList with status=1, orderBySort ASC
- addBanner: set tenantId from TenantInterceptor, linkType must be 1 or 2
- When linkType=1 (goods), validate goodsId exists and goodsType=1
- When linkType=2 (external), store externalUrl directly
</action>
  <verify>
    <automated>grep -c "class AdminBannerServiceImpl" zlt-business/mall-center/src/main/java/com/central/mall/service/impl/AdminBannerServiceImpl.java</automated>
  </verify>
  <done>AdminBannerService with full CRUD + sort methods, D-04 mixed link mode implemented</done>
</task>

<task type="auto">
  <name>Task 4: Create AdminBannerController</name>
  <files>zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminBannerController.java</files>
  <action>
Create AdminBannerController in controller/admin/ package:

Endpoints:
- GET /api/mall/admin/banner/list — getBannerList()
- POST /api/mall/admin/banner — addBanner(@RequestBody BannerDTO)
- PUT /api/mall/admin/banner — updateBanner(@RequestBody BannerDTO)
- DELETE /api/mall/admin/banner/{id} — deleteBanner(@PathVariable)
- PUT /api/mall/admin/banner/{id}/sort/{sort} — updateSort

Use @Tag(name = "管理员-轮播图管理"). All endpoints return Result.
</action>
  <verify>
    <automated>grep -c "@RequestMapping.*admin/banner" zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminBannerController.java</automated>
  </verify>
  <done>AdminBannerController with 5 endpoints for full CRUD + sort</done>
</task>

</tasks>

<verification>
## Verification

After execution:
1. Category endpoints: GET list returns tree, POST creates, PUT updates, DELETE removes, PUT sort reorders
2. Banner endpoints: GET list returns active banners, POST creates, PUT updates, DELETE removes, PUT sort updates
3. Banner linkType validation: linkType=1 requires valid goodsId, linkType=2 stores externalUrl
</verification>

<success_criteria>
- [ ] Category tree endpoint returns nested children structure
- [ ] Category delete cascades to children
- [ ] Banner mixed mode works (goodsId or externalUrl based on linkType)
- [ ] All endpoints respect tenant_id via TenantInterceptor
</success_criteria>

<output>
After completion, create `.planning/phases/02-后台管理-商品与系统/02-02-SUMMARY.md`
</output>
