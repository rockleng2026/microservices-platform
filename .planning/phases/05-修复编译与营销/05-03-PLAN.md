---
phase: "05"
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
files_modified:
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallCouponTemplate.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallCoupon.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallMarketingActivity.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallPointsAccount.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallPointsLog.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallOrder.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallCouponTemplateMapper.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallCouponMapper.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallMarketingActivityMapper.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallPointsAccountMapper.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallPointsLogMapper.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/service/IMarketingService.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/service/impl/MarketingServiceImpl.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/controller/CouponController.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/controller/MemberController.java"
autonomous: false
requirements:
  - "MARKETING-01"
  - "MARKETING-02"
  - "MARKETING-03"
  - "MARKETING-04"
  - "MARKETING-05"
  - "MARKETING-06"
  - "MARKETING-07"
  - "MARKETING-08"
  - "MARKETING-09"
---

<objective>
实现营销模块：优惠券管理（含满减券）、促销活动、会员积分系统
</objective>

<read_first>
- zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallOrder.java
- zlt-business/mall-center/src/main/java/com/central/mall/service/IOrderService.java
- zlt-business/mall-center/src/main/java/com/central/mall/service/impl/OrderServiceImpl.java
</read_first>

<tasks>

<task>
<read_first>
- zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallOrder.java
</read_first>
<action>
在 `MallOrder.java` 中添加优惠券相关字段：

```java
/** 使用的优惠券ID */
private Long couponId;
/** 优惠金额 */
private BigDecimal discountAmount;
```

更新对应的 mapper XML 和 DTO。
</action>
<acceptance_criteria>
- MallOrder.java 包含 couponId 和 discountAmount 字段
- MallOrder.xml (if exists) 包含新字段映射
</acceptance_criteria>
</task>

<task>
<read_first>
- zlt-business/mall-center/src/main/java/com/central/common/model/SuperEntity.java
</read_first>
<action>
创建营销模块实体类：

**MallCouponTemplate.java**（优惠券模板，管理员创建）:
```java
@TableName("mall_coupon_template")
public class MallCouponTemplate extends SuperEntity {
    private String name;           // 券名称
    private Integer type;         // 1=满减券,2=折扣券,3=无门槛券
    private BigDecimal faceValue;  // 面额（满减券）
    private BigDecimal discountRate; // 折扣率（折扣券，如0.85=85折）
    private BigDecimal minAmount;  // 最低消费金额
    private BigDecimal maxDiscount; // 最高优惠金额
    private Integer totalCount;   // 总数量
    private Integer remainCount;  // 剩余数量
    private Integer perUserLimit; // 每人限领数量
    private Integer validType;    // 1=固定日期,2=领取后N天
    private LocalDateTime startTime;
    private LocalDateTime endTime;
    private Integer validDays;    // 领取后有效天数
    private Integer status;       // 0=未发布,1=已发布,2=已下架
}
```

**MallCoupon.java**（用户已领取的优惠券）:
```java
@TableName("mall_coupon")
public class MallCoupon extends SuperEntity {
    private Long userId;
    private Long templateId;
    private String couponNo;      // 唯一标识
    private String name;
    private Integer type;
    private BigDecimal faceValue;
    private BigDecimal discountRate;
    private BigDecimal minAmount;
    private BigDecimal maxDiscount;
    private Long orderId;         // 关联使用的订单
    private Integer status;      // 1=未使用,2=已使用,3=已过期
    private LocalDateTime receiveTime;
    private LocalDateTime useTime;
    private LocalDateTime expireTime;
}
```

**MallMarketingActivity.java**（促销活动）:
```java
@TableName("mall_marketing_activity")
public class MallMarketingActivity extends SuperEntity {
    private String name;
    private Integer type;         // 1=满减,2=折扣,3=买赠
    private String ruleJson;      // 规则JSON: {"minAmount":100,"discountAmount":10}
    private LocalDateTime startTime;
    private LocalDateTime endTime;
    private Integer status;       // 1=待发布,2=进行中,3=已结束
    private Integer priority;     // 优先级
}
```

**MallPointsAccount.java**（用户积分账户）:
```java
@TableName("mall_points_account")
public class MallPointsAccount extends SuperEntity {
    private Long userId;
    private Integer balance;     // 当前余额
    private Integer totalEarned; // 累计获得
    private Integer totalSpent; // 累计消耗
}
```

**MallPointsLog.java**（积分变动日志）:
```java
@TableName("mall_points_log")
public class MallPointsLog extends SuperEntity {
    private Long userId;
    private Integer type;        // 1=获得,2=消耗
    private Integer points;
    private Integer balanceAfter; // 变动后余额
    private String source;       // ORDER,REFUND,REDEEM
    private String sourceId;     // 关联业务ID
    private String remark;
}
```

对应的 Mapper 接口（均继承 `BaseMapper`）：
- MallCouponTemplateMapper.java
- MallCouponMapper.java
- MallMarketingActivityMapper.java
- MallPointsAccountMapper.java
- MallPointsLogMapper.java
</action>
<acceptance_criteria>
- 5个实体类文件存在且包含所有必需字段
- 5个 Mapper 接口存在并继承 BaseMapper
</acceptance_criteria>
</task>

<task>
<read_first>
- zlt-business/mall-center/src/main/java/com/central/mall/service/IMarketingService.java
</read_first>
<action>
创建 `IMarketingService.java` 接口：

```java
public interface IMarketingService {
    // 优惠券
    Result<?> createCouponTemplate(CouponTemplateDTO dto, Long adminId);
    Result<?> publishCouponTemplate(Long templateId, Long adminId);
    Result<?> offlineCouponTemplate(Long templateId, Long adminId);
    Result<?> claimCoupon(Long templateId, Long userId);
    Result<?> getUserCouponList(Long userId, Integer status, PageDTO pageDTO);
    Result<?> getAvailableCoupons(Long userId);
    Result<?> validateCoupon(Long userId, Long templateId, BigDecimal orderAmount);
    Result<?> useCoupon(Long userId, Long couponId, Long orderId);
    Result<?> invalidateCoupon(Long couponId, Long orderId);  // 订单失败时回退
    
    // 促销活动
    Result<?> createActivity(MarketingActivityDTO dto, Long adminId);
    Result<?> getActivePromotions();
    Result<?> calculatePromotionDiscount(BigDecimal orderAmount, List<Long> goodsIds);
    
    // 会员积分
    Result<?> getMemberInfo(Long userId);
    Result<?> getPointsLog(Long userId, PageDTO pageDTO);
    Result<?> earnPointsOnOrderComplete(Long orderId);
    Result<?> deductPointsOnRefund(Long orderId);
}
```

创建 `MarketingServiceImpl.java`，核心逻辑：
1. **优惠券校验**：`validateCoupon` 需检查：
   - 优惠券是否在有效期内
   - 用户是否已达到限领上限
   - 订单金额是否满足最低消费
   - 使用 Redis 原子操作防止超领（`remainCount - 1`）

2. **优惠券互斥**：同一订单只能使用一种优惠（优惠券或促销活动二选一）

3. **积分计算**：订单完成后，积分 = 支付金额（按比例，如100:1）

4. **积分回退**：退款时从 `mall_points_log` 扣除相应积分
</action>
<acceptance_criteria>
- IMarketingService.java 包含所有接口方法声明
- MarketingServiceImpl.java 实现类存在
</acceptance_criteria>
</task>

<task>
<read_first>
- zlt-business/mall-center/src/main/java/com/central/mall/controller/CouponController.java
</read_first>
<action>
创建 `CouponController.java`：

```java
@RestController
@RequestMapping("/coupon")
public class CouponController {
    @Autowired
    private IMarketingService marketingService;
    
    // 用户端
    @GetMapping("/list")
    public Result<?> getUserCouponList(@LoginUser Long userId, 
        @RequestParam(required = false) Integer status, PageDTO pageDTO);
    
    @GetMapping("/available")
    public Result<?> getAvailableCoupons(@LoginUser Long userId, 
        @RequestParam BigDecimal orderAmount);
    
    @PostMapping("/{id}/claim")
    public Result<?> claimCoupon(@PathVariable Long id, @LoginUser Long userId);
    
    // 管理端
    @PostMapping("/admin/template")
    @SaRouteCheck(role = "admin")
    public Result<?> createTemplate(@RequestBody CouponTemplateDTO dto, @LoginUser Long adminId);
    
    @PutMapping("/admin/template/{id}")
    @SaRouteCheck(role = "admin")
    public Result<?> updateTemplate(@PathVariable Long id, @RequestBody CouponTemplateDTO dto, @LoginUser Long adminId);
    
    @PostMapping("/admin/template/{id}/publish")
    @SaRouteCheck(role = "admin")
    public Result<?> publishTemplate(@PathVariable Long id, @LoginUser Long adminId);
    
    @PostMapping("/admin/template/{id}/offline")
    @SaRouteCheck(role = "admin")
    public Result<?> offlineTemplate(@PathVariable Long id, @LoginUser Long adminId);
}
```

创建 `MemberController.java`：

```java
@RestController
@RequestMapping("/member")
public class MemberController {
    @Autowired
    private IMarketingService marketingService;
    
    // 用户端
    @GetMapping("/info")
    public Result<?> getMemberInfo(@LoginUser Long userId);
    
    @GetMapping("/points/log")
    public Result<?> getPointsLog(@LoginUser Long userId, PageDTO pageDTO);
    
    // 管理端
    @PutMapping("/admin/{id}/points")
    @SaRouteCheck(role = "admin")
    public Result<?> adjustPoints(@PathVariable Long id, @RequestBody AdjustPointsDTO dto, @LoginUser Long adminId);
}
```
</action>
<acceptance_criteria>
- CouponController.java 存在且包含所有用户端和管理端接口
- MemberController.java 存在且包含所有接口
- @SaRouteCheck(role = "admin") 注解存在于管理端接口
</acceptance_criteria>
</task>

<task>
<read_first>
- zlt-business/mall-center/src/main/java/com/central/mall/service/IOrderService.java
- zlt-business/mall-center/src/main/java/com/central/mall/service/impl/OrderServiceImpl.java
</read_first>
<action>
在 `OrderServiceImpl.java` 的 `createOrder` 方法中，在库存预占之前（marketing validation must happen BEFORE stock pre-allocation）集成优惠券和促销活动校验：

```java
// 1. 营销优惠校验（在库存预占之前）
BigDecimal discountAmount = BigDecimal.ZERO;
Long couponId = null;
if (dto.getCouponId() != null) {
    // 校验并使用优惠券
    DiscountResultDTO discountResult = marketingService.validateAndUseCoupon(
        dto.getCouponId(), userId, dto.getTotalAmount());
    discountAmount = discountResult.getDiscountAmount();
    couponId = dto.getCouponId();
} else if (dto.getPromotionId() != null) {
    // 校验并应用促销活动
    discountAmount = marketingService.calculatePromotionDiscount(
        dto.getPromotionId(), dto.getGoodsIdList());
}
// 实际支付金额 = 总金额 - 优惠金额
finalAmount = dto.getTotalAmount().subtract(discountAmount);

// 2. 库存预占 (在优惠校验之后)
// ...
```

注意：优惠券和促销活动互斥，同一订单只能使用一种。
</action>
<acceptance_criteria>
- OrderServiceImpl.java 在库存预占之前调用营销优惠校验
- 优惠券和促销活动互斥逻辑存在
</acceptance_criteria>
</task>

<task>
<action>
执行数据库迁移脚本：

```sql
-- 优惠券模板表
CREATE TABLE `mall_coupon_template` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `tenant_id` VARCHAR(32),
    `name` VARCHAR(64) NOT NULL,
    `type` TINYINT NOT NULL DEFAULT 1 COMMENT '1=满减券,2=折扣券,3=无门槛券',
    `face_value` DECIMAL(10,2),
    `discount_rate` DECIMAL(5,2) COMMENT 'e.g., 0.85=85折',
    `min_amount` DECIMAL(10,2) DEFAULT 0,
    `max_discount` DECIMAL(10,2),
    `total_count` INT NOT NULL,
    `remain_count` INT NOT NULL,
    `per_user_limit` INT DEFAULT 1,
    `valid_type` TINYINT DEFAULT 1 COMMENT '1=固定日期,2=领取后N天',
    `start_time` DATETIME,
    `end_time` DATETIME,
    `valid_days` INT,
    `status` TINYINT DEFAULT 0 COMMENT '0=未发布,1=已发布,2=已下架',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 用户优惠券表
CREATE TABLE `mall_coupon` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `tenant_id` VARCHAR(32),
    `user_id` BIGINT NOT NULL,
    `template_id` BIGINT NOT NULL,
    `coupon_no` VARCHAR(32) NOT NULL UNIQUE,
    `name` VARCHAR(64) NOT NULL,
    `type` TINYINT NOT NULL,
    `face_value` DECIMAL(10,2),
    `discount_rate` DECIMAL(5,2),
    `min_amount` DECIMAL(10,2),
    `max_discount` DECIMAL(10,2),
    `order_id` BIGINT COMMENT 'used by order',
    `status` TINYINT DEFAULT 1 COMMENT '1=未使用,2=已使用,3=已过期',
    `receive_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `use_time` DATETIME,
    `expire_time` DATETIME NOT NULL,
    KEY `idx_user_id` (`user_id`),
    KEY `idx_template_id` (`template_id`)
);

-- 促销活动表
CREATE TABLE `mall_marketing_activity` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `tenant_id` VARCHAR(32),
    `name` VARCHAR(128) NOT NULL,
    `type` TINYINT NOT NULL DEFAULT 1 COMMENT '1=满减,2=折扣,3=买赠',
    `rule_json` TEXT COMMENT '{"minAmount":100,"discountAmount":10}',
    `start_time` DATETIME NOT NULL,
    `end_time` DATETIME NOT NULL,
    `status` TINYINT DEFAULT 1,
    `priority` INT DEFAULT 0,
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 会员积分账户表
CREATE TABLE `mall_points_account` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `tenant_id` VARCHAR(32),
    `user_id` BIGINT NOT NULL UNIQUE,
    `balance` INT DEFAULT 0,
    `total_earned` INT DEFAULT 0,
    `total_spent` INT DEFAULT 0,
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 会员积分日志表
CREATE TABLE `mall_points_log` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `tenant_id` VARCHAR(32),
    `user_id` BIGINT NOT NULL,
    `type` TINYINT NOT NULL COMMENT '1=获得,2=消耗',
    `points` INT NOT NULL,
    `balance_after` INT NOT NULL,
    `source` VARCHAR(32) NOT NULL COMMENT 'ORDER,REFUND,REDEEM',
    `source_id` VARCHAR(64),
    `remark` VARCHAR(255),
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    KEY `idx_user_id` (`user_id`)
);

-- 订单表添加优惠券字段
ALTER TABLE `mall_order` ADD COLUMN `coupon_id` BIGINT AFTER `total_amount`;
ALTER TABLE `mall_order` ADD COLUMN `discount_amount` DECIMAL(10,2) DEFAULT 0 AFTER `coupon_id`;
```
</action>
<acceptance_criteria>
- mall_coupon_template 表创建成功
- mall_coupon 表创建成功
- mall_marketing_activity 表创建成功
- mall_points_account 表创建成功
- mall_points_log 表创建成功
- mall_order 表添加 coupon_id 和 discount_amount 字段成功
</acceptance_criteria>
</task>

</tasks>

<verification>
1. 编译通过：`mvn compile -pl zlt-business/mall-center -am -q`
2. 单元测试：所有 IMarketingService 方法存在且可编译
</verification>

<success_criteria>
- 优惠券全流程完整（创建→发布→领取→使用→互斥）
- 促销活动支持满减规则
- 会员积分：订单完成获得积分，退款扣减积分
</success_criteria>
