分析了飞书的多维表格又新增了一个表 mt_app_space 应用空间表
逻辑是用户创建多维表格，正确的理解是创建了一个应用空间，里面包含多个多维表格和视图
打开一个多维表格，其实是一个应用的整体 就像是一个独立的应用
所有我在table表和view增加了app_space_code的字段关联到mt_app_space
需求：
1、请根据最新的表结构 @multi_table_system.sql ,重新更新下后端工程的实体对象、映射文件、已经新增一个应用空间管理的接口
2、修改前端功能:前端工程登录成功后跳转到主页面时 主页面的url变成/base，原来调用api-multitable/api/tables访问多维表格列表的接口更换成获取应用空间的接口
