---
phase: "03"
plan: "04"
type: "execute"
wave: 4
depends_on:
  - "03-03"
files_modified:
  - "zlt-business/mall-center/src/main/java/com/central/mall/service/IAdminStockService.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/service/impl/AdminStockServiceImpl.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminStockController.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminExpressController.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/dto/StockCorrectDTO.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/dto/ExpressDTO.java"
autonomous: true
requirements:
  - "STOCK-04"
  - "STOCK-05"
  - "STOCK-06"
  - "DELIVERY-01"
user_setup: []

must_haves:
  truths:
    - "Admin can view all SKU stock levels via GET /api/mall/admin/stock"
    - "Admin can manually correct stock with reason via PUT /api/mall/admin/stock/{skuId}/correct"
    - "Stock correction logs operation with stockBefore/stockAfter"
    - "Admin can maintain logistics companies via CRUD endpoints"
    - "Stock below threshold triggers alert notification"
  artifacts:
    - path: "zlt-business/mall-center/src/main/java/com/central/mall/service/IAdminStockService.java"
      provides: "Admin stock view and correction interface"
    - path: "zlt-business/mall-center/src/main/java/com/central/mall/service/impl/AdminStockServiceImpl.java"
      provides: "Stock threshold alerts via notification service"
    - path: "zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminStockController.java"
      provides: "Admin stock management endpoints"
    - path: "zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminExpressController.java"
      provides: "Logistics company CRUD endpoints"
  key_links:
    - from: "AdminStockServiceImpl"
      to: "IStockService"
      via: "correctStock method"
    - from: "AdminStockServiceImpl"
      to: "NotificationService"
      via: "sendStockAlert when below threshold"
---

<objective>
Create admin stock management and logistics company maintenance endpoints, plus stock alert notification system.

Purpose: Complete remaining Phase 3 requirements: STOCK-04, STOCK-05, STOCK-06 (stock view, correction, alerts) and DELIVERY-01 (logistics config).

Output: Admin stock controller, admin express controller, DTOs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@zlt-business/mall-center/src/main/java/com/central/mall/service/impl/AdminGoodsServiceImpl.java
@zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallGoodsSku.java
@zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallExpress.java
@zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallStockLog.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IAdminStockService and AdminStockServiceImpl</name>
  <files>
    - "zlt-business/mall-center/src/main/java/com/central/mall/service/IAdminStockService.java"
    - "zlt-business/mall-center/src/main/java/com/central/mall/service/impl/AdminStockServiceImpl.java"
  </files>
  <action>
    Create IAdminStockService interface:
    - getSkuStockPage(page, params): IPage<SkuStockDTO> - admin stock list
    - getSkuStockDetail(skuId): SkuStockDTO - single SKU detail with stock log
    - correctStock(skuId, change, operator, remark): boolean
    - getStockThreshold(): Integer (from mall_settings)

    Create SkuStockDTO:
    - skuId, goodsId, goodsName, skuCode, specs (JSON)
    - price, stock, realStock (actual DB stock vs Redis)
    - stockStatus: String ("正常" if stock > threshold, "预警" if <= threshold, "无限制" if -1)
    - threshold: Integer
    - lastStockChangeTime, lastStockChangeType

    AdminStockServiceImpl implementation:
    - Inject MallGoodsSkuMapper, MallGoodsMapper, MallStockLogMapper, IStockService, TenantInterceptor
    
    getSkuStockPage:
    - LambdaQueryWrapper<MallGoodsSku> with tenantId filter
    - Join with MallGoods for goodsName
    - Support filtering by goodsId, keyword (goodsName/skuCode)
    - Map to SkuStockDTO with stockStatus computed

    correctStock (STOCK-05):
    - Call stockService.correctStock(skuId, change, operator, remark)
    - StockLog already written inside correctStock

    getStockAlertList (STOCK-06):
    - Query all SKUs where stock >= 0 AND stock <= threshold
    - Return list for notification trigger

    For stock alert notification (STOCK-06):
    - Create sendStockAlert(Long skuId) private method
    - Query threshold from mall_settings (key: "stock_alert_threshold", default 10)
    - If corrected stock <= threshold after correction, log alert
    - Note: Actual notification (email/SMS/WeChat) depends on Phase 4 notification system
    - For now: log.warn("Stock alert: SKU {} stock {} below threshold {}", skuId, stock, threshold)
  </action>
  <verify>
    <automated>grep -c "getSkuStockPage\|correctStock\|getStockAlertList" zlt-business/mall-center/src/main/java/com/central/mall/service/IAdminStockService.java</automated>
  </verify>
  <done>AdminStockServiceImpl provides stock list with threshold status, manual correction with audit log, and alert logging.</done>
</task>

<task type="auto">
  <name>Task 2: Create AdminStockController with stock endpoints</name>
  <files>
    - "zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminStockController.java"
  </files>
  <action>
    Create AdminStockController.java:
    - @RestController, @RequestMapping("/api/mall/admin/stock"), @RequiredArgsConstructor
    - @Tag(name = "管理员-库存管理")
    - Inject IAdminStockService

    Endpoints:
    1. GET /list (getSkuStockPage) - STOCK-04
       - @RequestParam(defaultValue = "1") Long page
       - @RequestParam(defaultValue = "20") Long pageSize
       - @RequestParam(required = false) Long goodsId
       - @RequestParam(required = false) String keyword
       - Returns IPage<SkuStockDTO>

    2. GET /{skuId} (getSkuStockDetail) - STOCK-04
       - @PathVariable Long skuId
       - Returns SkuStockDTO with recent stock logs (last 10 operations)

    3. PUT /{skuId}/correct (correctStock) - STOCK-05
       - @PathVariable Long skuId
       - @RequestBody StockCorrectDTO dto
       - Validates change != 0
       - Calls adminStockService.correctStock(skuId, dto.getChange(), dto.getOperator(), dto.getRemark())
       - Returns Result.succeed(true) or Result.failed("修正失败")

    4. GET /alert/list (getStockAlertList) - STOCK-06
       - Returns list of SKUs below threshold
       - For notification trigger (Phase 4 integration)

    Use TenantInterceptor.getCurrentTenantId() for multi-tenant filtering.
    All list responses return IPage with pagination.
  </action>
  <verify>
    <automated>grep -c "getSkuStockPage\|getSkuStockDetail\|correctStock" zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminStockController.java</automated>
  </verify>
  <done>AdminStockController provides all admin stock management endpoints. Multi-tenant filtering applied.</done>
</task>

<task type="auto">
  <name>Task 3: Create AdminExpressController for logistics company CRUD (DELIVERY-01)</name>
  <files>
    - "zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminExpressController.java"
    - "zlt-business/mall-center/src/main/java/com/central/mall/model/dto/ExpressDTO.java"
    - "zlt-business/mall-center/src/main/java/com/central/mall/model/dto/StockCorrectDTO.java"
  </files>
  <action>
    Create ExpressDTO.java:
    - @Data
    - id (Long, optional for create)
    - name: String (required, e.g., "顺丰速运")
    - code: String (required, e.g., "SF", "YTO")
    - logo: String (optional, URL to logo)
    - sort: Integer (optional, default 0)
    - status: Integer (0=disabled, 1=enabled, default 1)

    Create StockCorrectDTO.java:
    - @Data
    - change: Integer (required, positive=increase, negative=decrease)
    - operator: String (required, admin name performing correction)
    - remark: String (optional, reason for correction)

    Create AdminExpressController.java:
    - @RestController, @RequestMapping("/api/mall/admin/express"), @RequiredArgsConstructor
    - @Tag(name = "管理员-物流公司管理")
    - Inject MallExpressMapper

    Endpoints:
    1. GET /list (getExpressList) - DELIVERY-01
       - Returns list of all express companies for current tenant
       - Filter by status=1 for dropdown selection

    2. GET /{id} (getExpressDetail)
       - Returns single express company detail

    3. POST / (createExpress) - DELIVERY-01
       - @RequestBody ExpressDTO dto
       - Validates name, code required
       - Checks code uniqueness within tenant
       - Creates MallExpress record

    4. PUT / (updateExpress) - DELIVERY-01
       - @RequestBody ExpressDTO dto (with id)
       - Updates existing record

    5. DELETE /{id} (deleteExpress)
       - Soft delete (set status=0)

    6. PUT /{id}/status/{status} (updateStatus)
       - Enable/disable express company

    Note: Express list is used by AdminOrderController.shipOrder() dropdown.
    Pre-populate with common express companies: 顺丰(SF), 中通(ZTO), 圆通(YTO), 韵达(YD), 申通(STO).
  </action>
  <verify>
    <automated>grep -c "getExpressList\|createExpress\|updateExpress" zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminExpressController.java</automated>
  </verify>
  <done>AdminExpressController provides full CRUD for logistics companies. Express list used in admin order shipping dropdown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {Admin Panel -> API} | Admin actions cross this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Tampering | Stock correction exploit | mitigate | Only admin role can correct; all corrections logged in MallStockLog |
| T-03-14 | Denial | Stock alert spam | accept | Alerts are logged; actual notification is Phase 4 concern |
| T-03-15 | Information | Express API key leak | mitigate | Express accounts not stored; just name/code/logo for dropdown |
</threat_model>

<verification>
- IAdminStockService has getSkuStockPage, correctStock, getStockAlertList
- AdminStockController compiles with 4 endpoints
- AdminExpressController compiles with 6 endpoints (CRUD + status toggle)
- StockCorrectDTO has change, operator, remark fields
- ExpressDTO has name, code, logo, sort, status fields
</verification>

<success_criteria>
- Admin can view paginated SKU stock list with threshold status (STOCK-04)
- Admin can correct stock with positive/negative change and mandatory operator/remark (STOCK-05)
- Stock correction writes MallStockLog with operationType=4
- Stock below threshold logs alert warning (STOCK-06)
- Admin can CRUD logistics companies (DELIVERY-01)
- Express list dropdown works in admin order shipping
</success_criteria>

<output>
After completion, create `.planning/phases/03-订单与支付核心/03-04-SUMMARY.md`
</output>
