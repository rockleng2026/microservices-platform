CREATE TABLE `soo_financial_model_base` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '模型ID',
  `model_code` varchar(50) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '模型编码',
  `model_name` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '模型名称',
  `model_version` varchar(20) COLLATE utf8mb4_unicode_ci DEFAULT '1.0.0' COMMENT '模型版本',
  `model_category` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '模型分类',
  `model_description` text COLLATE utf8mb4_unicode_ci COMMENT '模型描述',
  `is_template` tinyint(1) DEFAULT '0' COMMENT '是否为模板',
  `publish_status` tinyint(1) DEFAULT '0' COMMENT '发布状态 0-草稿状态，1-已发布，2-历史版本',
  `model_config` json DEFAULT NULL COMMENT '模型配置',
  `validation_rules` json DEFAULT NULL COMMENT '验证规则',
  `creator_id` bigint(20) DEFAULT NULL COMMENT '创建人ID',
  `creator_name` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '创建人姓名',
  `tenant_id` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '租户ID',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `model_code` (`model_code`，`'model_version`),
  KEY `idx_model_code` (`model_code`),
  KEY `idx_tenant_creator` (`tenant_id`,`creator_id`)
) ENGINE=InnoDB AUTO_INCREMENT=8 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='财务模型表V3';

CREATE TABLE `soo_financial_model_variable` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '变量ID',
  `model_id` bigint(20) NOT NULL COMMENT '模型ID',
  `variable_code` varchar(50) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '变量编码',
  `variable_name` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '变量名称',
  `variable_type` enum('INPUT','CALC','API') COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '变量类型：输入/计算/API',
  `data_type` enum('PERCENTAGE','CURRENCY') COLLATE utf8mb4_unicode_ci DEFAULT 'DECIMAL' COMMENT '数据类型',
  `unit` varchar(20) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '单位',
  `parent_id` bigint(20) DEFAULT NULL COMMENT '父级树',
  `default_value` decimal(20,6) DEFAULT NULL COMMENT '默认值',
  `min_value` decimal(20,6) DEFAULT NULL COMMENT '最小值',
  `max_value` decimal(20,6) DEFAULT NULL COMMENT '最大值',
  `calculation_formula` text COLLATE utf8mb4_unicode_ci COMMENT '计算公式',
  `api_config` json DEFAULT NULL COMMENT 'API配置',
  `display_order` int(11) DEFAULT '0' COMMENT '显示顺序',
  `is_required` tinyint(1) DEFAULT '0' COMMENT '是否必填',
  `is_key_indicator` tinyint(1) DEFAULT '0' COMMENT '是否关键指标',
  `is_visible` tinyint(1) DEFAULT '1' COMMENT '是否显示',
  `validation_rules` json DEFAULT NULL COMMENT '验证规则',
  `description` text COLLATE utf8mb4_unicode_ci COMMENT '变量描述',
  `help_text` text COLLATE utf8mb4_unicode_ci COMMENT '帮助说明',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_model_variable` (`model_id`,`variable_code`),
  KEY `idx_model_type` (`model_id`,`variable_type`),
  KEY `idx_model_order` (`model_id`,`display_order`),
  KEY `idx_model_variable_type_order` (`model_id`,`variable_type`,`display_order`),
  CONSTRAINT `soo_model_variable_ibfk_1` FOREIGN KEY (`model_id`) REFERENCES `soo_financial_model_base` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB AUTO_INCREMENT=42 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='模型变量表';

CREATE TABLE `soo_financial_model_variable_dependency` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '依赖ID',
  `model_id` bigint(20) NOT NULL COMMENT '模型ID',
  `variable_id` bigint(20) NOT NULL COMMENT '变量ID',
  `dependent_variable_id` bigint(20) NOT NULL COMMENT '依赖的变量ID',
  `dependency_type` enum('DIRECT','INDIRECT') COLLATE utf8mb4_unicode_ci DEFAULT 'DIRECT' COMMENT '依赖类型',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_variable_dependency` (`variable_id`,`dependent_variable_id`),
  KEY `idx_model_variable` (`model_id`,`variable_id`),
  KEY `dependent_variable_id` (`dependent_variable_id`),
  CONSTRAINT `soo_variable_dependency_ibfk_1` FOREIGN KEY (`model_id`) REFERENCES `soo_financial_model_base` (`id`) ON DELETE CASCADE,
  CONSTRAINT `soo_variable_dependency_ibfk_2` FOREIGN KEY (`variable_id`) REFERENCES `soo_financial_model_variable` (`id`) ON DELETE CASCADE,
  CONSTRAINT `soo_variable_dependency_ibfk_3` FOREIGN KEY (`dependent_variable_id`) REFERENCES `soo_financial_model_variable` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB AUTO_INCREMENT=16 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='变量依赖关系表';


soo_financial_model_instance

发布状态
草稿 已发布 编辑时创建一份新的草稿主表 和变量表

变量的取值逻辑，如果calculation_formula有值 则从公式中取值，如果calculation_formula为空则从默认值中取值