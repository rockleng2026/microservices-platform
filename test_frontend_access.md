# 前端访问测试指南

## 🎯 测试目标
验证从财务模型管理页面到模型实例管理页面的跳转功能

## 📋 测试步骤

### 1. 访问财务模型管理页面
- 打开浏览器访问: http://localhost:8001/saleops-optimizer/financial-analysis/financial-models
- 确认页面正常加载，显示模型列表

### 2. 查找实例管理图标
- 在模型列表的操作列中，应该能看到以下按钮：
  - 👁️ 查看详情
  - ✏️ 编辑
  - ⚙️ 变量管理
  - 📦 **实例管理** (新增的图标)
  - 📊 图表配置
  - 📋 复制
  - 📥 导出
  - 🗑️ 删除

### 3. 点击实例管理图标
- 点击任意模型行的"实例管理"图标
- 应该跳转到: http://localhost:8001/saleops-optimizer/financial-analysis/model-instances?modelId=1
- URL中应该包含modelId参数

### 4. 验证实例管理页面
- 确认页面正常加载
- 检查是否只显示该模型的实例
- 验证创建新实例时，模型ID是否自动填充

## 🔧 功能验证

### 实例管理页面功能
1. **实例列表显示** - 应该显示该模型的所有实例
2. **创建新实例** - 点击"新建实例"按钮
3. **实例详情查看** - 点击实例行的"查看详情"
4. **变量编辑** - 点击"变量编辑"按钮
5. **计算执行** - 点击"执行计算"按钮
6. **实例克隆** - 点击"克隆"按钮

### URL参数传递验证
- 从模型管理页面跳转时，URL应该包含modelId参数
- 实例管理页面应该根据modelId过滤显示实例
- 创建新实例时，模型ID应该自动填充

## ✅ 成功标志

- ✅ 财务模型管理页面正常加载
- ✅ 操作列中显示"实例管理"图标
- ✅ 点击图标正确跳转到实例管理页面
- ✅ URL包含正确的modelId参数
- ✅ 实例管理页面根据modelId过滤显示
- ✅ 创建新实例时模型ID自动填充

## 🐛 问题排查

### 如果图标不显示
1. 检查浏览器控制台是否有JavaScript错误
2. 确认前端服务正常启动
3. 检查组件是否正确导入AppstoreOutlined图标

### 如果跳转失败
1. 检查路由配置是否正确
2. 确认ModelInstanceManagement组件存在
3. 检查URL参数传递是否正确

### 如果页面加载失败
1. 检查后端API是否正常响应
2. 确认数据库连接正常
3. 查看浏览器网络请求状态

## 🎉 完成测试

一旦所有测试通过，说明从财务模型管理到模型实例管理的跳转功能已经正常工作！

用户现在可以：
1. 在财务模型管理页面查看所有模型
2. 点击任意模型的"实例管理"图标
3. 跳转到该模型的实例管理页面
4. 管理该模型的所有实例 