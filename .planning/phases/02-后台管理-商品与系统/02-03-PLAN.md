# Phase 2 Plan 03: 商品发布与SKU规格管理

**Phase:** 02-后台管理-商品与系统
**Plan:** 02-03
**Type:** execute
**Wave:** 3
**Depends On:** [02-01, 02-02] (needs entities, mappers, DTOs, and category service)
**Files Modified:**
- `zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminGoodsController.java` (NEW)
- `zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminSpecController.java` (NEW)
- `zlt-business/mall-center/src/main/java/com/central/mall/service/IAdminGoodsService.java` (NEW)
- `zlt-business/mall-center/src/main/java/com/central/mall/service/IAdminSpecService.java` (NEW)
- `zlt-business/mall-center/src/main/java/com/central/mall/service/impl/AdminGoodsServiceImpl.java` (NEW)
- `zlt-business/mall-center/src/main/java/com/central/mall/service/impl/AdminSpecServiceImpl.java` (NEW)
**Autonomous:** true
**Requirements:** GOODS-06, GOODS-08, VIRTUAL-02, VIRTUAL-03, VIRTUAL-04

---

<objective>
Implement admin goods publishing/editing with multi-SKU support, virtual goods configuration, and SKU specification value pool management. Supports D-01 (图片先上传后选择), D-02 (预定义规格+规格值池), D-03 (虚拟商品固定库存+人工补货), D-12 (商品克隆).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@D:\code\microservices-platform\.planning\ROADMAP.md
@D:\code\microservices-platform\.planning\phases\02-后台管理-商品与系统\02-CONTEXT.md

## Implementation Decisions from 02-CONTEXT.md

| D-ID | Decision | Implementation |
|------|----------|----------------|
| D-01 | 图片上传方式 | Images already uploaded to OSS; save goods with direct URL reference in mainImage/images fields |
| D-02 | SKU规格预定义+值池 | Admin selects from mall_spec (预定义规格如颜色/内存) and mall_spec_value (规格值如红色/16GB) when publishing |
| D-03 | 虚拟商品库存 | Virtual goods: stock >= 0 fixed inventory; when stock=0, admin manually replenishes via mall_resource_delivery |
| D-12 | 商品克隆 | Clone button copies goods info including SKUs; new goodsId assigned |

## Entity References (Phase 1 + 02-01)

**MallGoods** (existing): id, tenantId, categoryId, name, subTitle, mainImage, images, detail, price, sales, status, sort, goodsType (1=physical,2=virtual), virtualUrl, virtualFileId, virtualExpire, delFlag, createTime, updateTime

**MallGoodsSku** (existing): id, tenantId, goodsId, skuCode, specs (JSON), price, stock (-1=unlimited), image, status, createTime, updateTime

**MallGoodsSpec** (existing): id, tenantId, goodsId, specName, specValues (JSON array), createTime

**MallSpec** (02-01): id, tenantId, specName, createTime

**MallSpecValue** (02-01): id, tenantId, specId, specValue, createTime

## AdminGoodsDTO fields (from 02-01)

id, categoryId, name, subTitle, mainImage, images, detail, price, goodsType, virtualUrl, virtualFileId, virtualExpire, skus (List<SkuDTO>), status, sort
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IAdminGoodsService and AdminGoodsServiceImpl</name>
  <files>
    zlt-business/mall-center/src/main/java/com/central/mall/service/IAdminGoodsService.java
    zlt-business/mall-center/src/main/java/com/central/mall/service/impl/AdminGoodsServiceImpl.java
  </files>
  <action>
**IAdminGoodsService.java** methods:
- getGoodsPage(page, params): IPage<AdminGoodsDTO> — admin goods list with filters (categoryId, keyword, status, goodsType)
- getGoodsDetail(id): AdminGoodsDTO — full goods detail with SKUs
- publishGoods(dto): boolean — create new goods, handle SKU creation
- updateGoods(dto): boolean — update goods and replace SKUs entirely
- deleteGoods(id): boolean — soft delete (delFlag=1)
- updateStatus(id, status): boolean — single goods status toggle
- batchUpdateStatus(goodsIds, status): boolean — D-09: 50件/批, process in sub-batches of 50
- cloneGoods(goodsId, newCategoryId, newName): Long — D-12: return new goodsId

**AdminGoodsServiceImpl.java**:
- extends ServiceImpl<MallGoodsMapper, MallGoods>
- implements IAdminGoodsService
- Inject MallGoodsMapper, MallGoodsSkuMapper, MallGoodsSpecMapper
- publishGoods: insert MallGoods, then insert each SKU (specs JSON stored as {"specName":"value"}), insert spec definitions
- updateGoods: delete existing SKUs and specs for goodsId, then insert new
- batchUpdateStatus: iterate goodsIds in batches of 50 (D-09), use LambdaUpdateWrapper
- cloneGoods: query original goods, copy fields to new MallGoods with new id, copy SKUs with new goodsId, return new goodsId
- getGoodsPage: status filter (null = all), delFlag=0 only
</action>
  <verify>
    <automated>grep -c "class AdminGoodsServiceImpl" zlt-business/mall-center/src/main/java/com/central/mall/service/impl/AdminGoodsServiceImpl.java</automated>
  </verify>
  <done>AdminGoodsService with publish/update/delete/batchStatus/clone methods</done>
</task>

<task type="auto">
  <name>Task 2: Create AdminGoodsController</name>
  <files>zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminGoodsController.java</files>
  <action>
Create AdminGoodsController in controller/admin/ package:

Endpoints:
- GET /api/mall/admin/goods/list — getGoodsPage (page, pageSize, categoryId, keyword, status, goodsType)
- GET /api/mall/admin/goods/{id} — getGoodsDetail
- POST /api/mall/admin/goods — publishGoods(@RequestBody AdminGoodsDTO)
- PUT /api/mall/admin/goods — updateGoods(@RequestBody AdminGoodsDTO)
- DELETE /api/mall/admin/goods/{id} — deleteGoods (soft delete)
- PUT /api/mall/admin/goods/{id}/status/{status} — updateStatus
- PUT /api/mall/admin/goods/batch/status — batchUpdateStatus(@RequestBody BatchStatusDTO)
- POST /api/mall/admin/goods/clone — cloneGoods(@RequestBody GoodsCloneDTO)

Use @Tag(name = "管理员-商品管理"). Return Result. Validate: name required, categoryId required, goodsType required (1 or 2), price required.
</action>
  <verify>
    <automated>grep -c "@RequestMapping.*admin/goods" zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminGoodsController.java</automated>
  </verify>
  <done>AdminGoodsController with 8 endpoints for full goods management</done>
</task>

<task type="auto">
  <name>Task 3: Create IAdminSpecService and AdminSpecServiceImpl for SKU spec value pool</name>
  <files>
    zlt-business/mall-center/src/main/java/com/central/mall/service/IAdminSpecService.java
    zlt-business/mall-center/src/main/java/com/central/mall/service/impl/AdminSpecServiceImpl.java
  </files>
  <action>
**IAdminSpecService.java** methods:
- getSpecList(): List<SpecDTO> — all specs with their values
- addSpec(specName): Long — create new spec definition, return specId
- addSpecValue(specId, specValue): Long — add value to spec's value pool
- deleteSpec(specId): boolean — delete spec and all its values
- deleteSpecValue(valueId): boolean — delete single spec value

**AdminSpecServiceImpl.java**:
- extends ServiceImpl<MallSpecMapper, MallSpec>
- implements IAdminSpecService
- Inject MallSpecMapper, MallSpecValueMapper
- getSpecList: select MallSpec list, for each spec select MallSpecValue list by specId, build SpecDTO
- addSpec: insert MallSpec with tenantId
- addSpecValue: insert MallSpecValue with tenantId and specId
- deleteSpec: delete all MallSpecValue where specId = id, then delete MallSpec
- deleteSpecValue: delete by id only
</action>
  <verify>
    <automated>grep -c "class AdminSpecServiceImpl" zlt-business/mall-center/src/main/java/com/central/mall/service/impl/AdminSpecServiceImpl.java</automated>
  </verify>
  <done>AdminSpecService with spec and spec value pool CRUD</done>
</task>

<task type="auto">
  <name>Task 4: Create AdminSpecController</name>
  <files>zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminSpecController.java</files>
  <action>
Create AdminSpecController in controller/admin/ package:

Endpoints:
- GET /api/mall/admin/spec/list — getSpecList (returns all specs with values)
- POST /api/mall/admin/spec — addSpec(@RequestBody Map with "specName")
- POST /api/mall/admin/spec/value — addSpecValue(@RequestBody Map with "specId", "specValue")
- DELETE /api/mall/admin/spec/{id} — deleteSpec
- DELETE /api/mall/admin/spec/value/{id} — deleteSpecValue

Use @Tag(name = "管理员-SKU规格管理"). Return Result.
</action>
  <verify>
    <automated>grep -c "@RequestMapping.*admin/spec" zlt-business/mall-center/src/main/java/com/central/mall/controller/admin/AdminSpecController.java</automated>
  </verify>
  <done>AdminSpecController with 5 endpoints for spec value pool management</done>
</task>

</tasks>

<verification>
## Verification

After execution:
1. Goods publish: creates goods + SKUs + spec definitions in database
2. Goods edit: replaces SKUs entirely (delete old, insert new)
3. Batch status: 100 goods processed as 2 batches of 50
4. Clone: new goods created with copied SKUs and new goodsId
5. Spec pool: specs and values stored in mall_spec and mall_spec_value
</verification>

<success_criteria>
- [ ] publishGoods creates MallGoods + MallGoodsSku entries + MallGoodsSpec entries
- [ ] updateGoods removes old SKUs and inserts new ones
- [ ] batchUpdateStatus processes in sub-batches of 50 (D-09)
- [ ] cloneGoods returns new goodsId with copied SKUs
- [ ] getSpecList returns all specs with their values nested
</success_criteria>

<output>
After completion, create `.planning/phases/02-后台管理-商品与系统/02-03-SUMMARY.md`
</output>
