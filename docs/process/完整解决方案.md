# Portal OAuth 认证完整解决方案

## 问题分析

测试结果显示：
1. ✅ 客户端认证已成功（使用 webApp/webApp 客户端）
2. ❌ 用户认证失败："用户名或密码错误"

## 根本原因

Portal用户认证需要从 `central_organization.users` 表查询用户，但该表可能：
1. 不存在
2. 没有测试用户数据

## 解决方案

### 1. 创建并初始化central_organization数据库

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS `central_organization` DEFAULT CHARACTER SET = utf8;
USE `central_organization`;

-- 创建用户表
CREATE TABLE `users` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `username` varchar(50) NOT NULL COMMENT '用户名',
  `password` varchar(100) NOT NULL COMMENT '密码',
  `nickname` varchar(50) DEFAULT NULL COMMENT '昵称',
  `head_img_url` varchar(200) DEFAULT NULL COMMENT '头像',
  `mobile` varchar(20) DEFAULT NULL COMMENT '手机号',
  `sex` tinyint(1) DEFAULT '0' COMMENT '性别 0-男 1-女',
  `enabled` tinyint(1) DEFAULT '1' COMMENT '是否启用',
  `type` varchar(20) DEFAULT 'APP' COMMENT '用户类型',
  `del_flag` int(1) DEFAULT '0' COMMENT '删除标记',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `username` (`username`),
  KEY `mobile` (`mobile`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='用户表';

-- 插入测试用户（密码使用BCrypt加密）
INSERT INTO `users` (`username`, `password`, `nickname`, `enabled`, `type`) VALUES
('admin', '$2a$10$A8h5S.QfSUk1Pt2j6HPYHOxPGKH4z2ySgVkD8Z9LLK8NjOaDy2z6u', '系统管理员', 1, 'APP'),
('lihua', '$2a$10$A8h5S.QfSUk1Pt2j6HPYHOxPGKH4z2ySgVkD8Z9LLK8NjOaDy2z6u', '李华', 1, 'APP'),
('wangxh', '$2a$10$A8h5S.QfSUk1Pt2j6HPYHOxPGKH4z2ySgVkD8Z9LLK8NjOaDy2z6u', '王小华', 1, 'APP');
```

### 2. 修改前端认证配置

由于 `portal-web` 客户端配置有问题，暂时使用 `webApp` 客户端：

**在 `zlt-web/portal-web/src/pages/Login/index.tsx` 中：**

```typescript
const loginData = {
  grant_type: 'password',
  username: values.username,
  password: values.password,
  client_id: 'webApp',        // 使用webApp客户端
  client_secret: 'webApp',    // webApp密钥
  account_type: 'portal'      // Portal用户类型
};
```

### 3. 测试登录

启动前端：
```bash
cd zlt-web/portal-web
npm run dev
```

访问：http://127.0.0.1:8065

使用测试账号：
- 用户名：admin
- 密码：admin123

### 4. 后续优化

如果需要使用 `portal-web` 客户端，需要：

1. 检查数据库中客户端配置
2. 确保client_secret正确匹配
3. 重启UAA服务清除缓存

## 技术要点

1. **客户端认证**：使用HTTP Basic认证方式
2. **用户认证**：Portal用户从 `central_organization.users` 表查询
3. **密码加密**：使用BCrypt，明文密码 `admin123` 对应加密串 `$2a$10$A8h5S.QfSUk1Pt2j6HPYHOxPGKH4z2ySgVkD8Z9LLK8NjOaDy2z6u`

## 服务状态检查

- UAA服务：http://127.0.0.1:9900 ✅
- OAuth Token API：http://127.0.0.1:9900/api-uaa/oauth/token ✅
- 前端服务：http://127.0.0.1:8065 