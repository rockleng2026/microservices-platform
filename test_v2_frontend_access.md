# V2版本前端访问测试指南

## 🎯 测试目标
验证V2版本财务模型管理页面到模型实例管理页面的跳转功能

## 📋 测试步骤

### 1. 访问V2版本财务模型管理页面
- 打开浏览器访问: http://localhost:8001/saleops-optimizer/financial-analysis/v2/financial-models
- 确认页面正常加载，显示"财务模型管理 V2"标题
- 确认显示模型列表

### 2. 查找实例管理图标
- 在模型列表的操作列中，应该能看到以下按钮：
  - 👁️ 查看详情
  - ✏️ 编辑
  - ⚙️ 变量管理
  - 📦 **实例管理** (新增的图标)
  - 📊 图表配置
  - 📋 复制
  - 📥 导出JSON
  - 📥 导出Excel
  - 🗑️ 删除

### 3. 点击实例管理图标
- 点击任意模型行的"📦 实例管理"图标
- 应该在新标签页中打开: http://localhost:8001/saleops-optimizer/financial-analysis/model-instances?modelId=1
- URL中应该包含modelId参数

### 4. 验证实例管理页面
- 确认新标签页正常加载模型实例管理页面
- 检查是否只显示该模型的实例
- 验证创建新实例时，模型ID是否自动填充

## 🔧 功能验证

### V2版本特性
1. **新标签页打开** - 实例管理页面在新标签页中打开，不影响原页面
2. **URL参数传递** - 正确传递modelId参数到实例管理页面
3. **页面标题** - 显示"财务模型管理 V2"标题
4. **操作按钮** - 包含所有V2版本的操作按钮

### 实例管理页面功能
1. **实例列表显示** - 应该显示该模型的所有实例
2. **创建新实例** - 点击"新建实例"按钮
3. **实例详情查看** - 点击实例行的"查看详情"
4. **变量编辑** - 点击"变量编辑"按钮
5. **计算执行** - 点击"执行计算"按钮
6. **实例克隆** - 点击"克隆"按钮

## ✅ 成功标志

- ✅ V2版本财务模型管理页面正常加载
- ✅ 页面标题显示"财务模型管理 V2"
- ✅ 操作列中显示"📦 实例管理"图标
- ✅ 点击图标在新标签页中打开实例管理页面
- ✅ URL包含正确的modelId参数
- ✅ 实例管理页面根据modelId过滤显示
- ✅ 创建新实例时模型ID自动填充

## 🐛 问题排查

### 如果图标不显示
1. 检查浏览器控制台是否有JavaScript错误
2. 确认前端服务正常启动
3. 检查组件是否正确导入AppstoreOutlined图标
4. 确认访问的是V2版本页面

### 如果跳转失败
1. 检查路由配置是否正确
2. 确认ModelInstanceManagement组件存在
3. 检查URL参数传递是否正确
4. 确认新标签页没有被浏览器阻止

### 如果页面加载失败
1. 检查后端API是否正常响应
2. 确认数据库连接正常
3. 查看浏览器网络请求状态
4. 检查新标签页的URL是否正确

## 🎉 完成测试

一旦所有测试通过，说明V2版本从财务模型管理到模型实例管理的跳转功能已经正常工作！

用户现在可以：
1. 在V2版本财务模型管理页面查看所有模型
2. 点击任意模型的"📦 实例管理"图标
3. 在新标签页中打开该模型的实例管理页面
4. 管理该模型的所有实例，同时保持原页面状态

## 📝 版本对比

| 功能 | V1版本 | V2版本 |
|------|--------|--------|
| 页面标题 | 财务模型管理 | 财务模型管理 V2 |
| 实例管理跳转 | 当前页面跳转 | 新标签页打开 |
| 图标位置 | 变量管理后 | 变量管理后 |
| 路由路径 | /financial-models | /v2/financial-models | 