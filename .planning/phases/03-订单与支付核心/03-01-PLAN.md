---
phase: "03"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallOrder.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallOrderItem.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallDelivery.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallResourceDelivery.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallStockLog.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallOrderMapper.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallOrderItemMapper.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallDeliveryMapper.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallResourceDeliveryMapper.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallStockLogMapper.java"
autonomous: true
requirements:
  - "ORDER-01"
  - "ORDER-02"
  - "STOCK-01"
  - "STOCK-04"
  - "STOCK-05"
  - "STOCK-06"
  - "DELIVERY-01"
user_setup: []

must_haves:
  truths:
    - "MallOrder entity exists with all required fields (userId, addressId, status, goodsType, totalAmount)"
    - "MallOrderItem entity stores order line items with skuId, quantity, price"
    - "MallDelivery entity stores logistics info (waybillNo, expressCode, expressName)"
    - "MallResourceDelivery entity stores virtual product delivery (resourceUrl, expireTime)"
    - "MallStockLog entity records all stock operations (skuId, change, type, operator)"
  artifacts:
    - path: "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallOrder.java"
      provides: "Order entity with status enum (1=pending pay, 2=paid, 3=shipped, 4=completed, 5=cancelled)"
    - path: "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallOrderItem.java"
      provides: "Order item entity linked to orderId"
    - path: "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallDelivery.java"
      provides: "Delivery entity for logistics tracking"
    - path: "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallResourceDelivery.java"
      provides: "Virtual product delivery records"
    - path: "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallStockLog.java"
      provides: "Audit trail for stock changes"
    - path: "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallOrderMapper.java"
      provides: "MyBatis-Plus mapper for MallOrder"
  key_links:
    - from: "MallOrder"
      to: "MallOrderItem"
      via: "one-to-many via orderId"
    - from: "MallOrder"
      to: "MallDelivery"
      via: "one-to-one via orderId"
    - from: "MallOrderItem"
      to: "MallGoodsSku"
      via: "skuId foreign key"
    - from: "MallStockLog"
      to: "MallGoodsSku"
      via: "skuId foreign key"
---

<objective>
Create database entities and mappers for Phase 3 order and payment system. This is the foundation that all subsequent waves depend on.

Purpose: Establish the data model for orders, order items, delivery, resource delivery, and stock logs.

Output: 5 entity files and 5 mapper interfaces following existing mall-center patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallGoods.java
@zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallGoodsSku.java
@zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallExpress.java
@zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallGoodsMapper.java
@zlt-business/mall-center/src/main/resources/application.yml

<interfaces>
<!-- Key entity fields that downstream services will use -->

MallOrder (status values):
  - 1 = pending pay (待付款)
  - 2 = paid (已付款)
  - 3 = shipped (已发货)
  - 4 = completed (已完成)
  - 5 = cancelled (已取消)

MallOrder (key fields):
  - id, tenantId, userId, orderNo (unique)
  - addressId (FK to mall_user_address, nullable for virtual goods)
  - goodsType (1=physical, 2=virtual)
  - totalAmount, freightAmount, payAmount
  - status, payTime, shipTime, completeTime
  - remark, delFlag
  - createTime, updateTime

MallOrderItem (key fields):
  - id, tenantId, orderId, userId
  - skuId, goodsId
  - goodsName, skuSpecs (JSON), goodsImage
  - price, quantity, subtotal

MallDelivery (key fields):
  - id, tenantId, orderId
  - expressCode, expressName
  - waybillNo, shipTime

MallResourceDelivery (key fields):
  - id, tenantId, orderId, orderItemId
  - userId, resourceUrl
  - expireTime, deliveryTime

MallStockLog (operation types):
  - 1 = pre-allocate (预占)
  - 2 = real deduction (真实扣减)
  - 3 = release (释放/回滚)
  - 4 = manual correction (手动修正)

MallStockLog (key fields):
  - id, tenantId, skuId, orderId
  - change (positive or negative)
  - stockBefore, stockAfter
  - operationType, operator
  - remark, createTime
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MallOrder entity and MallOrderMapper</name>
  <files>
    - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallOrder.java"
    - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallOrderMapper.java"
  </files>
  <action>
    Create MallOrder.java entity following existing MallGoods pattern:
    - Use @Data, @TableName("mall_order")
    - Fields: id (Long, AUTO), tenantId (String), userId (Long), orderNo (String, unique)
    - addressId (Long, nullable - virtual goods don't need address)
    - goodsType (Integer: 1=physical, 2=virtual)
    - totalAmount (BigDecimal), freightAmount (BigDecimal), payAmount (BigDecimal)
    - status (Integer: 1=pending pay, 2=paid, 3=shipped, 4=completed, 5=cancelled)
    - payTime (LocalDateTime, nullable), shipTime (nullable), completeTime (nullable)
    - remark (String), delFlag (Integer)
    - createTime, updateTime (LocalDateTime)
    
    Create MallOrderMapper.java as MyBatis-Plus BaseMapper interface.
  </action>
  <verify>
    <automated>cd /d/code/microservices-platform && javac -d /tmp/compile-test -cp "$(find ~/.m2/repository -name '*.jar' 2>/dev/null | tr '\n' ';')" zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallOrder.java 2>&1 || echo "Compilation check via IDE required"</automated>
  </verify>
  <done>MallOrder entity compiles with correct fields and status enum values. MallOrderMapper extends BaseMapper.</done>
</task>

<task type="auto">
  <name>Task 2: Create MallOrderItem entity and MallOrderItemMapper</name>
  <files>
    - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallOrderItem.java"
    - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallOrderItemMapper.java"
  </files>
  <action>
    Create MallOrderItem.java entity:
    - @Data, @TableName("mall_order_item")
    - Fields: id (Long, AUTO), tenantId (String), orderId (Long), userId (Long)
    - skuId (Long), goodsId (Long)
    - goodsName (String), skuSpecs (String, JSON), goodsImage (String)
    - price (BigDecimal), quantity (Integer), subtotal (BigDecimal)
    - createTime, updateTime (LocalDateTime)
    
    Create MallOrderItemMapper.java as MyBatis-Plus BaseMapper.
  </action>
  <verify>
    <automated>grep -l "class MallOrderItem" zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallOrderItem.java</automated>
  </verify>
  <done>MallOrderItem entity has all required fields for order line items with price, quantity, subtotal.</done>
</task>

<task type="auto">
  <name>Task 3: Create MallDelivery, MallResourceDelivery, MallStockLog entities and mappers</name>
  <files>
    - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallDelivery.java"
    - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallResourceDelivery.java"
    - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallStockLog.java"
    - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallDeliveryMapper.java"
    - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallResourceDeliveryMapper.java"
    - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallStockLogMapper.java"
  </files>
  <action>
    Create MallDelivery.java:
    - @Data, @TableName("mall_delivery")
    - Fields: id, tenantId, orderId (Long)
    - expressCode (String), expressName (String)
    - waybillNo (String), shipTime (LocalDateTime)
    - createTime, updateTime

    Create MallResourceDelivery.java:
    - @Data, @TableName("mall_resource_delivery")
    - Fields: id, tenantId, orderId, orderItemId, userId (all Long)
    - resourceUrl (String), expireTime (LocalDateTime, nullable)
    - deliveryTime (LocalDateTime), createTime

    Create MallStockLog.java:
    - @Data, @TableName("mall_stock_log")
    - Fields: id, tenantId, skuId, orderId (all Long)
    - change (Integer, positive for add, negative for subtract)
    - stockBefore (Integer), stockAfter (Integer)
    - operationType (Integer: 1=pre-allocate, 2=real deduction, 3=release, 4=manual)
    - operator (String, nullable), remark (String, nullable)
    - createTime (LocalDateTime)

    Create all three mapper interfaces extending BaseMapper.
  </action>
  <verify>
    <automated>grep -l "class MallDelivery\|class MallResourceDelivery\|class MallStockLog" zlt-business/mall-center/src/main/java/com/central/mall/model/entity/*.java</automated>
  </verify>
  <done>All three entities and mappers created with correct fields matching ROADMAP specifications.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {application -> database} | Application writes to mall_order tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tamper | mall_order.orderNo | mitigate | orderNo must be unique; use UUID or snowflake, never predictable sequence |
| T-03-02 | Denial | StockLog injection | accept | Log-only table; no business impact from flood |
| T-03-03 | Information | MallResourceDelivery leak | mitigate | resourceUrl should not be exposed in list APIs; only in detail for owner |
</threat_model>

<verification>
- All 5 entities compile without errors
- All 5 mappers extend BaseMapper
- Status enum values match: 1=pending pay, 2=paid, 3=shipped, 4=completed, 5=cancelled
- goodsType enum values match: 1=physical, 2=virtual
- operationType enum values match: 1=pre-allocate, 2=real deduction, 3=release, 4=manual
</verification>

<success_criteria>
- MallOrder entity exists with orderNo (unique), userId, addressId (nullable), goodsType, status, payAmount, payTime, shipTime, completeTime
- MallOrderItem entity linked to orderId with skuId, quantity, price, subtotal
- MallDelivery entity for logistics with waybillNo, expressCode, expressName
- MallResourceDelivery entity for virtual goods with resourceUrl, expireTime
- MallStockLog entity with skuId, change, operationType, stockBefore/After for audit
- All mappers extend MyBatis-Plus BaseMapper
- No duplicate fields or typos
</success_criteria>

<output>
After completion, create `.planning/phases/03-订单与支付核心/03-01-SUMMARY.md`
</output>
