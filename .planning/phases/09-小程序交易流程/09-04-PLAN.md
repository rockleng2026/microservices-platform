---
phase: "09"
slug: "小程序交易流程"
plan: "04"
type: "execute"
wave: 2
depends_on:
  - "09-01"
  - "09-03"
files_modified:
  - "mall-mini-program/src/pages/order-list/index.vue"
  - "mall-mini-program/src/pages/order-detail/index.vue"
autonomous: true
requirements:
  - "MINI-06-01"
  - "MINI-06-02"
  - "MINI-06-03"
  - "MINI-06-04"
  - "MINI-06-05"
  - "MINI-07-01"
  - "MINI-07-02"
  - "MINI-07-03"
  - "MINI-07-04"
  - "MINI-07-05"
user_setup: []
must_haves:
  truths:
    - "User can see 5 tabs: 全部/待付款/待发货/待收货/已完成"
    - "User can pull down to refresh current tab order list"
    - "User can scroll up to load more orders (pagination)"
    - "Each order card shows: order number, status badge, item thumbnails, total amount, created time"
    - "User can tap order card to navigate to order detail"
    - "Order detail shows: status banner, items, address, payment info, logistics"
    - "Order detail page has copy button for order number"
    - "Cancel button only visible when order status is 待付款"
    - "Confirm receipt button only visible when order status is 待收货"
  artifacts:
    - path: "mall-mini-program/src/pages/order-list/index.vue"
      provides: "Order list with 5 tabs, pull-refresh, infinite scroll"
      min_lines: 200
    - path: "mall-mini-program/src/pages/order-detail/index.vue"
      provides: "Order detail with status banner, items, address, payment, logistics, action buttons"
      min_lines: 200
  key_links:
    - from: "order-list/index.vue"
      to: "/pages/order-detail/index"
      via: "navigateTo with orderId as query"
    - from: "order-list/index.vue"
      to: "/api/mall/order/list"
      via: "GET with status filter and pagination params"
    - from: "order-detail/index.vue"
      to: "/api/mall/order/{id}"
      via: "GET order detail"
    - from: "order-detail/index.vue"
      to: "/pages/refund/apply"
      via: "navigateTo with orderId as query"
---

<objective>
Implement order list page (order-list/index.vue) with 5 tabs and pagination, and order detail page (order-detail/index.vue) with status-based action buttons. Order list shows all orders with pull-to-refresh and load-more. Order detail shows full order info in a fixed layout order.
</objective>

<context>
@mall-mini-program/src/config/api.ts
@mall-mini-program/src/pages/product-detail/index.vue
@.planning/phases/08-Admin基础框架与小程序首页商品/08-UI-SPEC.md
@.planning/phases/09-小程序交易流程/09-CONTEXT.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From mall-mini-program/src/config/api.ts:
```typescript
export const API_BASE = '/mall-center'
// To add:
export const ORDER_LIST = '/api/mall/order/list'
export const ORDER_DETAIL = '/api/mall/order'
```

Order data model:
```typescript
interface Order {
  id: number
  orderNo: string
  userId: number
  status: 'pending_payment' | 'paid' | 'shipped' | 'delivered' | 'completed' | 'cancelled' | 'refunding'
  totalAmount: number
  discountAmount: number
  freightFee: number
  finalAmount: number
  remark?: string
  createdAt: string
  paidAt?: string
  shippedAt?: string
  deliveredAt?: string
  items: OrderItem[]
  address: Address
  logistics?: LogisticsInfo
}

interface OrderItem {
  id: number
  orderId: number
  skuId: number
  goodsName: string
  goodsImage: string
  price: number
  quantity: number
  specs?: string
}
```

Status mapping for tabs:
```typescript
// Tab index → status filter
0: undefined (all)
// 1: pending_payment
// 2: paid (pending shipment)
// 3: shipped (pending receipt)
// 4: delivered or completed
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build order-list/index.vue — 5 tabs with pagination</name>
  <files>mall-mini-program/src/pages/order-list/index.vue</files>
  <read_first>
    - mall-mini-program/src/config/api.ts
    - mall-mini-program/src/pages/product-detail/index.vue
    - 08-UI-SPEC.md (for tabs, card styling)
  </read_first>
  <action>
Build pages/order-list/index.vue with 5 tabs and pagination:

**API endpoints to add in config/api.ts:**
```
ORDER_LIST = '/api/mall/order/list'
```

**Page layout:**

1. **Sticky tab bar** (5 tabs):
   - Tabs: "全部" | "待付款" | "待发货" | "待收货" | "已完成"
   - Per D-13: 5 tabs total
   - Active tab: accent color #ff5500, underline or bold
   - Scroll horizontally if tabs overflow (uni-tabs component or scroll-view)

2. **Order list** (scroll-view below tabs):
   - Each page load: 10 items
   - Per D-14: pull-to-refresh (uni.startPullDownRefresh) + load-more (scroll-view @scrolltolower)
   - Show loading indicator during fetch
   - Show empty state when no orders in current tab

3. **Order card** (white card, 8px radius, margin 8px):
   - **Header row**: "订单号: {orderNo}" + copy icon button (uni.setClipboardData)
   - **Status badge**: colored tag (using status map):
     - pending_payment: orange "待付款"
     - paid: blue "待发货"
     - shipped: blue "运输中"
     - delivered: green "待收货"
     - completed: green "已完成"
     - cancelled: gray "已取消"
     - refunding: red "退款中"
   - **Items row**: horizontal scroll or wrap of item thumbnails (40x40 each)
     - Tap any thumbnail → navigateTo /pages/product-detail/index?id={skuId}
   - **Footer row**: "实付: ¥{finalAmount}" (accent price) + created time (gray)
   - **Action buttons** (right side of footer):
     - If pending_payment: "取消" button (destructive #ff4d4f) + "去支付" button (accent)
     - If delivered: "确认收货" button (accent)
   - Tap anywhere on card (except buttons) → navigateTo /pages/order-detail/index?orderId={orderId}

4. **Tab switch behavior:**
   - On tab change: reset page=1, clear list, fetch new data
   - Show loading during fetch
   - Scroll back to top on tab switch

5. **API calls:**
   - GET /api/mall/order/list?status={status}&page={page}&pageSize=10
     - status param: 1=pending_payment, 2=paid, 3=shipped, 4=delivered+completed, omit for all
   - DELETE /api/mall/order/{id} — cancel order (MINI-06-04)
   - PUT /api/mall/order/{id}/confirm — confirm receipt (MINI-06-05)

**Status badge colors (per UI-SPEC):**
- pending_payment: background #fff7e6, text #fa8c16 (orange)
- paid/shipped: background #e6f7ff, text #1890ff (blue)
- delivered: background #f6ffed, text #52c41a (green)
- completed: background #f6ffed, text #52c41a (green)
- cancelled: background #f5f5f5, text #999999 (gray)
- refunding: background #fff1f0, text #ff4d4f (red)

**Cancel order flow:**
- Tap "取消" → confirm dialog "确定要取消该订单吗？"
- Confirm → DELETE /api/mall/order/{id}
- Success → show toast "订单已取消" → refresh list

**Confirm receipt flow:**
- Tap "确认收货" → confirm dialog "确认收到货物吗？"
- Confirm → PUT /api/mall/order/{id}/confirm
- Success → show toast "已确认收货" → refresh list

**Styling per 08-UI-SPEC:**
- Tab bar: sticky, white bg, border-bottom 1px #f0f0f0
- Active tab: #ff5500 text + 2px bottom border
- Cards: white bg, 8px radius, shadow
- Price: #ff5500, bold
- Badge: pill shape, small text
  </action>
  <verify>
    <automated>
grep -c "待付款\|待发货\|待收货\|已完成\|uni.startPullDownRefresh\|scroll-view.*@scrolltolower\|navigateTo.*order-detail" mall-mini-program/src/pages/order-list/index.vue
    </automated>
  </verify>
  <done>Order list page shows 5 tabs, pull-to-refresh, load-more pagination, order cards with status badges and action buttons</done>
</task>

<task type="auto">
  <name>Task 2: Build order-detail/index.vue — full order info with action buttons</name>
  <files>mall-mini-program/src/pages/order-detail/index.vue</files>
  <read_first>
    - mall-mini-program/src/config/api.ts
    - mall-mini-program/src/pages/product-detail/index.vue
    - 08-UI-SPEC.md
  </read_first>
  <action>
Build pages/order-detail/index.vue with full order info:

**API endpoints:**
```
ORDER_DETAIL = '/api/mall/order'
```

**Page layout (per D-15 order): status → items → address → payment → actions**

1. **Status banner** (top, full width, colored by status):
   - Large status text centered: "待付款" / "待发货" / "运输中" / "待收货" / "已完成" / "已取消"
   - Status description below: e.g., "请在30分钟内完成支付"
   - Background color matches status (same color as badge in order-list)

2. **Order info card** (white card):
   - Order number + copy button
   - Created time
   - Tap to copy order number: uni.setClipboardData({ data: orderNo })

3. **Logistics section** (only if status is shipped/delivered):
   - "物流信息" header
   - Logistics company name + tracking number + copy button
   - Timeline of delivery events (from logistics API)
   - Per MINI-07-02: logistics tracking timeline

4. **Order items section** (white card):
   - "商品信息" header
   - List of items: image (60x60) + name + specs + price + quantity
   - Tap item → navigateTo /pages/product-detail/index?id={skuId}

5. **Address section**:
   - "收货信息" header
   - Receiver name + phone
   - Full address: province + city + district + detail
   - If virtual goods order: show "无需收货" placeholder

6. **Payment info section**:
   - "支付信息" header
   - 商品总价: ¥{totalAmount}
   - 运费: ¥{freightFee}
   - 优惠: -¥{discountAmount}
   - 实付金额: ¥{finalAmount} (accent, bold, larger)
   - If paid: payment time

7. **Bottom action bar** (fixed 50px, white bg, shadow top):
   - Left side: "合计: ¥{finalAmount}" (accent price)
   - Right side buttons (status-dependent, per D-16):
     - pending_payment: "取消订单" (destructive #ff4d4f outline) + "去支付" (accent filled)
     - paid: (no buttons, just "等待发货中")
     - shipped: "查看物流" (outline) + "确认收货" (accent filled)
     - delivered: "评价" (outline) + "申请退款" (destructive outline)
     - completed: "评价" (outline) + "申请退款" (destructive outline)
     - refunding: "查看退款进度" (outline)
   - Per D-16: "取消订单" only shows for pending_payment
   - Tap "申请退款" → navigateTo /pages/refund/apply?orderId={orderId}
   - Tap "去支付" → navigateTo /pages/payment/index?orderId={orderId}

**Order number copy:**
- Use uni.setClipboardData({ data: order.orderNo })
- Show toast "订单号已复制" on success
- Per MINI-07-05: copy order number

**API calls:**
- GET /api/mall/order/{orderId} — fetch order detail
- PUT /api/mall/order/{orderId}/confirm — confirm receipt
- DELETE /api/mall/order/{orderId} — cancel order (only pending_payment)

**Styling per 08-UI-SPEC:**
- Status banner: 80px height, centered text, white text on colored bg
- Sections: white cards, 8px radius, 16px spacing
- Section headers: 14px, gray, uppercase
- Price display: accent #ff5500, bold
- Action buttons: 44px height, border-radius 22px
  </action>
  <verify>
  <automated>
grep -c "ORDER_DETAIL\|状态banner\|商品信息\|收货信息\|支付信息\|取消订单\|确认收货\|申请退款" mall-mini-program/src/pages/order-detail/index.vue
  </automated>
  <done>Order detail page displays all order info sections in correct order, with correct action buttons per status</done>
</task>

</tasks>

<verification>
- Order list shows 5 tabs with correct labels
- Pull-to-refresh calls API with current tab's status filter
- Scroll-to-bottom loads next page (10 items per page)
- Order card shows order number, status badge, thumbnails, price, created time
- Tapping card navigates to order detail
- Cancel button visible only for pending_payment orders
- Confirm receipt button visible only for shipped/delivered orders
- Order detail shows status banner, items, address, payment info, logistics
- Order number copy works
- Refund button navigates to refund apply page
</verification>

<success_criteria>
- MINI-06-01: 5 tabs displayed (全部/待付款/待发货/待收货/已完成)
- MINI-06-02: Order card shows order number, status, items, total, created time
- MINI-06-03: Tap card navigates to order detail
- MINI-06-04: Cancel order works for pending_payment orders
- MINI-06-05: Confirm receipt works for shipped orders
- MINI-07-01: Full order info displayed in correct section order
- MINI-07-02: Logistics timeline shown for shipped/delivered
- MINI-07-03: Evaluate button shown for delivered/completed
- MINI-07-04: Refund button shown for delivered/completed
- MINI-07-05: Order number can be copied
</success_criteria>

<output>
After completion, create .planning/phases/09-小程序交易流程/09-04-SUMMARY.md
</output>
