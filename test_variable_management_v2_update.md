# 变量管理V2页面CALC_FACTORS类型功能测试

## 🎯 测试目标

验证变量管理V2页面 `saleops-optimizer/financial-analysis/v2/variable-management?modelId=7` 中新增修改变量时，变量类型下拉选项包含"计算因子"选项，并且相关功能正常工作。

## ✅ 已完成的更新

### 1. 变量类型选项更新
- ✅ 在 `variableTypeOptions` 数组中添加了 `{ label: '计算因子', value: 'CALC_FACTORS' }`
- ✅ 变量类型选择器现在包含四种类型：输入、计算、计算因子、API

### 2. 表单字段显示规则更新
- ✅ **CALC类型**: 显示"计算表达式"字段
- ✅ **API类型**: 显示"API配置"字段  
- ✅ **INPUT/CALC_FACTORS类型**: 显示"默认值"字段
- ✅ **所有类型**: 都显示"约束条件公式"字段

### 3. 表格显示更新
- ✅ 变量类型标签颜色映射：
  - INPUT: 蓝色
  - CALC: 绿色
  - CALC_FACTORS: 紫色
  - API: 橙色

## 📋 测试步骤

### 1. 访问变量管理页面

**测试URL**: http://localhost:8001/saleops-optimizer/financial-analysis/v2/variable-management?modelId=7

**预期结果**: 页面正常加载，显示变量列表

### 2. 测试新增变量功能

**测试步骤**:
1. 点击"新增变量"按钮
2. 在弹出的表单中，点击"变量类型"下拉框
3. 验证下拉选项包含：
   - 输入
   - 计算
   - 计算因子
   - API

**预期结果**: 变量类型下拉框显示所有四种选项

### 3. 测试不同变量类型的表单字段显示

**测试步骤**:
1. 选择"输入"类型
   - 验证显示"默认值"字段
   - 验证不显示"计算表达式"字段
   - 验证不显示"API配置"字段

2. 选择"计算"类型
   - 验证显示"计算表达式"字段
   - 验证不显示"默认值"字段
   - 验证不显示"API配置"字段

3. 选择"计算因子"类型
   - 验证显示"默认值"字段
   - 验证不显示"计算表达式"字段
   - 验证不显示"API配置"字段

4. 选择"API"类型
   - 验证显示"API配置"字段
   - 验证不显示"默认值"字段
   - 验证不显示"计算表达式"字段

**预期结果**: 每种变量类型都显示正确的表单字段

### 4. 测试约束条件公式字段

**测试步骤**:
1. 选择任意变量类型
2. 验证"约束条件公式"字段始终显示
3. 在约束条件公式字段中输入内容

**预期结果**: 约束条件公式字段在所有变量类型下都显示

### 5. 测试变量创建和保存

**测试数据**:
```json
{
  "variableCode": "test_calc_factors",
  "variableName": "测试计算因子",
  "variableType": "CALC_FACTORS",
  "dataType": "PERCENTAGE",
  "defaultValue": 25,
  "unit": "%",
  "constraintFormula": "test_calc_factors <= 100",
  "description": "测试计算因子变量"
}
```

**测试步骤**:
1. 填写上述测试数据
2. 点击"创建"按钮
3. 验证变量创建成功
4. 验证新创建的变量在列表中正确显示

**预期结果**: 变量创建成功，列表中显示紫色"计算因子"标签

### 6. 测试变量编辑功能

**测试步骤**:
1. 点击刚创建的变量的"编辑"按钮
2. 验证表单正确加载所有字段值
3. 修改变量类型为其他类型
4. 验证表单字段根据新类型正确切换
5. 保存修改

**预期结果**: 编辑功能正常，表单字段切换正确

### 7. 测试表格显示

**测试步骤**:
1. 查看变量列表中的变量类型标签
2. 验证不同变量类型的颜色：
   - 输入变量：蓝色标签
   - 计算变量：绿色标签
   - 计算因子变量：紫色标签
   - API变量：橙色标签

**预期结果**: 变量类型标签颜色正确显示

## 🔧 技术验证

### 1. 前端代码验证
```typescript
// 验证变量类型选项配置
const variableTypeOptions = [
  { label: '输入', value: 'INPUT' },
  { label: '计算', value: 'CALC' },
  { label: '计算因子', value: 'CALC_FACTORS' }, // ✅ 新增
  { label: 'API', value: 'API' }
];

// 验证条件渲染逻辑
if (variableType === 'CALC') {
  // 显示计算表达式
} else if (variableType === 'API') {
  // 显示API配置
} else if (variableType === 'INPUT' || variableType === 'CALC_FACTORS') {
  // 显示默认值
}
```

### 2. 变量类型标签颜色验证
```typescript
// 验证标签颜色映射
color={record.variableType === 'INPUT' ? 'blue' : 
       record.variableType === 'CALC' ? 'green' : 
       record.variableType === 'CALC_FACTORS' ? 'purple' : 'orange'}
```

## 🐛 常见问题

### 1. 变量类型下拉框不显示"计算因子"
**问题**: 下拉选项中缺少"计算因子"选项
**解决**: 检查 `variableTypeOptions` 数组配置

### 2. 表单字段显示错误
**问题**: CALC_FACTORS类型显示错误的字段
**解决**: 检查条件渲染逻辑中的变量类型判断

### 3. 标签颜色显示错误
**问题**: CALC_FACTORS类型标签颜色不正确
**解决**: 检查表格列定义中的颜色映射逻辑

### 4. 保存失败
**问题**: 创建CALC_FACTORS类型变量时保存失败
**解决**: 检查后端API是否支持CALC_FACTORS类型

## 📝 测试检查清单

- [ ] 变量类型下拉框包含"计算因子"选项
- [ ] 选择"计算因子"类型时显示"默认值"字段
- [ ] 选择"计算因子"类型时不显示"计算表达式"字段
- [ ] 选择"计算因子"类型时不显示"API配置"字段
- [ ] 约束条件公式字段在所有类型下都显示
- [ ] 创建CALC_FACTORS类型变量成功
- [ ] 表格中CALC_FACTORS类型显示紫色标签
- [ ] 编辑CALC_FACTORS类型变量功能正常
- [ ] 变量类型切换时表单字段正确切换
- [ ] 所有变量类型的标签颜色正确

## 🎉 完成标准

所有测试项目通过，变量管理V2页面正确支持CALC_FACTORS类型，用户体验良好。 