---
phase: "04"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallEvaluate.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallEvaluateMapper.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/service/IEvaluateService.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/service/impl/EvaluateServiceImpl.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/controller/EvaluateController.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/dto/EvaluateDTO.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/dto/EvaluateListDTO.java"
autonomous: true
requirements:
  - "EVAL-01"
  - "EVAL-02"
  - "EVAL-03"
user_setup:
  - service: file-center
    why: "Image uploads via /files-anon multipart endpoint"
    env_vars:
      - name: FILE_CENTER_URL
        source: "From application.yml - already configured for file-center on port 5000"

must_haves:
  truths:
    - "User can submit evaluation for completed order items (status=4)"
    - "Duplicate evaluation per orderItem is rejected with unique constraint"
    - "User can view their evaluation history with pagination"
    - "Evaluation images stored as JSON array via file-center"
  artifacts:
    - path: "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallEvaluate.java"
      provides: "Evaluation entity with orderItemId unique constraint, star(1-5), content(500chars), images(JSON array)"
    - path: "zlt-business/mall-center/src/main/java/com/central/mall/service/IEvaluateService.java"
      provides: "submitEvaluate, listUserEvaluates, listGoodsEvaluates methods"
    - path: "zlt-business/mall-center/src/main/java/com/central/mall/controller/EvaluateController.java"
      provides: "POST /api/mall/evaluate, GET /api/mall/evaluate, GET /api/mall/goods/{id}/evaluates"
  key_links:
    - from: "EvaluateServiceImpl"
      to: "MallOrder"
      via: "Order.status must be 4 (completed) for evaluation eligibility"
    - from: "EvaluateServiceImpl"
      to: "MallOrderItem"
      via: "orderItemId links evaluation to specific SKU"
    - from: "EvaluateController"
      to: "file-center"
      via: "Image upload via /files-anon multipart"
---

<objective>
Create evaluation module for user product reviews. Users can submit evaluations for completed orders and view their evaluation history.

Purpose: Implement EVAL-01 (submit evaluation with rating, comment, images), EVAL-02 (view own evaluations), EVAL-03 (display evaluations on goods detail).

Output: MallEvaluate entity, IEvaluateService, EvaluateController, DTOs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallOrder.java
@zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallOrderItem.java
@zlt-business/mall-center/src/main/java/com/central/mall/service/impl/OrderServiceImpl.java
@zlt-business/mall-center/src/main/java/com/central/mall/controller/OrderController.java

<interfaces>
<!-- MallEvaluate entity structure from RESEARCH.md -->
MallEvaluate fields:
  - id (Long, AUTO)
  - tenantId (String)
  - orderId (Long)           -- For queries by order
  - orderItemId (Long)       -- UNIQUE constraint, points to specific SKU purchased
  - goodsId (Long)           -- For goods-level aggregation
  - userId (Long)
  - star (Integer, 1-5)
  - content (String, max 500 chars)
  - images (String)          -- JSON array ["url1","url2"]
  - createTime (LocalDateTime)
  - updateTime (LocalDateTime)

MallOrder status values (from MallOrder.java):
  - 1 = pending pay (待付款)
  - 2 = paid (已付款)
  - 3 = shipped (已发货)
  - 4 = completed (已完成)
  - 5 = cancelled (已取消)

<!-- Existing mapper patterns -->
MallOrderMapper extends BaseMapper<MallOrder>
MallOrderItemMapper extends BaseMapper<MallOrderItem>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MallEvaluate entity and MallEvaluateMapper</name>
  <files>
    - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallEvaluate.java"
    - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallEvaluateMapper.java"
  </files>
  <action>
    Create MallEvaluate.java:
    - @Data, @TableName("mall_evaluate")
    - Fields: id (Long, AUTO), tenantId (String), orderId (Long), orderItemId (Long, unique index)
    - goodsId (Long), userId (Long), star (Integer), content (String, max 500)
    - images (String, TEXT - stores JSON array of URLs)
    - createTime, updateTime (LocalDateTime)
    - Add @TableId(type = IdType.AUTO) on id

    Create MallEvaluateMapper.java:
    - extends BaseMapper<MallEvaluate>
    - No custom methods needed for v1 (MyBatis-Plus LambdaQueryWrapper handles all queries)

    The unique index on orderItemId prevents duplicate evaluations per order item.
    Database migration SQL:
    CREATE TABLE mall_evaluate (
      id BIGINT AUTO_INCREMENT PRIMARY KEY,
      tenant_id VARCHAR(50),
      order_id BIGINT NOT NULL,
      order_item_id BIGINT NOT NULL UNIQUE,
      goods_id BIGINT NOT NULL,
      user_id BIGINT NOT NULL,
      star INT NOT NULL CHECK (star BETWEEN 1 AND 5),
      content VARCHAR(500),
      images TEXT,
      create_time DATETIME,
      update_time DATETIME,
      INDEX idx_order_id (order_id),
      INDEX idx_goods_id (goods_id),
      INDEX idx_user_id (user_id)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
  </action>
  <verify>
    <automated>grep -l "class MallEvaluate" zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallEvaluate.java && grep -l "interface MallEvaluateMapper" zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallEvaluateMapper.java</automated>
  </verify>
  <done>MallEvaluate entity has all required fields. orderItemId has unique constraint. MallEvaluateMapper extends BaseMapper.</done>
</task>

<task type="auto">
  <name>Task 2: Create IEvaluateService and EvaluateServiceImpl</name>
  <files>
    - "zlt-business/mall-center/src/main/java/com/central/mall/service/IEvaluateService.java"
    - "zlt-business/mall-center/src/main/java/com/central/mall/service/impl/EvaluateServiceImpl.java"
  </files>
  <action>
    Create IEvaluateService interface:
    - submitEvaluate(Long userId, EvaluateDTO dto): boolean  -- EVAL-01
    - listUserEvaluates(Long userId, Integer page, Integer pageSize): IPage<EvaluateListDTO>  -- EVAL-02
    - listGoodsEvaluates(Long goodsId, Integer page, Integer pageSize): IPage<EvaluateListDTO>  -- EVAL-03

    Create EvaluateDTO in model/dto/:
    - @Data
    - orderId: Long (required)
    - orderItemId: Long (required)
    - goodsId: Long (required)
    - star: Integer (required, 1-5)
    - content: String (optional, max 500 chars)
    - images: List<String> (optional, max 9 URLs)

    Create EvaluateListDTO in model/dto/:
    - @Data
    - id, orderId, orderItemId, goodsId, userId
    - star: Integer
    - content: String
    - images: List<String> (parsed from JSON)
    - goodsName: String (from MallGoods join)
    - userNickname: String (from sys_user - default "匿名用户")
    - createTime: LocalDateTime

    EvaluateServiceImpl implementation:
    - Inject MallEvaluateMapper, MallOrderMapper, MallOrderItemMapper, MallGoodsMapper, TenantInterceptor

    submitEvaluate (EVAL-01):
    1. Validate order exists and belongs to user (order.userId == userId)
    2. Validate order.status == 4 (completed) - throw "订单未完成，无法评价"
    3. Check orderItemId belongs to order - throw "订单项不匹配"
    4. Check not already evaluated: selectCount where orderItemId = dto.orderItemId > 0 - throw "已评价过此商品"
    5. Insert MallEvaluate record with star, content, images (JSON stringify)
    6. Return true

    listUserEvaluates (EVAL-02):
    - LambdaQueryWrapper<MallEvaluate> filter by userId
    - Order by createTime DESC
    - Paginated IPage
    - Join MallGoods for goodsName in result

    listGoodsEvaluates (EVAL-03):
    - LambdaQueryWrapper<MallEvaluate> filter by goodsId
    - Order by createTime DESC
    - Paginated IPage
    - Join MallGoods for goodsName, anonymous user display

    Image upload note: Frontend uploads images to /files-anon separately, receives URL array, stores in evaluate.images JSON field. EvaluateController does NOT handle multipart upload - it only receives URLs.
  </action>
  <verify>
    <automated>grep -c "submitEvaluate\|listUserEvaluates\|listGoodsEvaluates" zlt-business/mall-center/src/main/java/com/central/mall/service/IEvaluateService.java</automated>
  </verify>
  <done>EvaluateServiceImpl validates order completion status before evaluation. Duplicate evaluation rejected. Pagination works for user and goods evaluation lists.</done>
</task>

<task type="auto">
  <name>Task 3: Create EvaluateController with API endpoints</name>
  <files>
    - "zlt-business/mall-center/src/main/java/com/central/mall/controller/EvaluateController.java"
  </files>
  <action>
    Create EvaluateController.java:
    - @RestController, @RequestMapping("/api/mall/evaluate"), @RequiredArgsConstructor
    - @Tag(name = "商品评价", description = "小程序端评价接口")
    - Inject IEvaluateService
    - getCurrentUserId() returns 1L stub (same as CartController/OrderController)

    Endpoints:
    1. POST / (submitEvaluate) - EVAL-01
       - @RequestBody EvaluateDTO dto
       - Validates star in range 1-5
       - Validates content max 500 chars
       - Validates images list max 9 items
       - Calls evaluateService.submitEvaluate(userId, dto)
       - Returns Result.succeed(true) or Result.failed(message)

    2. GET / (listUserEvaluates) - EVAL-02
       - @RequestParam(defaultValue = "1") Integer page
       - @RequestParam(defaultValue = "10") Integer pageSize
       - Calls evaluateService.listUserEvaluates(userId, page, pageSize)
       - Returns paginated result

    3. GET /goods/{goodsId}/evaluates (listGoodsEvaluates) - EVAL-03
       - @PathVariable Long goodsId
       - @RequestParam(defaultValue = "1") Integer page
       - @RequestParam(defaultValue = "10") Integer pageSize
       - Calls evaluateService.listGoodsEvaluates(goodsId, page, pageSize)
       - Returns paginated result

    Additional endpoint for image upload (via file-center):
    4. POST /images (uploadImages) - returns URLs for frontend to attach
       - NOTE: This proxies to file-center /files-anon
       - Frontend should call file-center directly; this endpoint is optional convenience
       - For optional implementation only - skip if complexity exceeds context budget

    Use Result.succeed() and Result.failed() consistently.
    All user-facing endpoints use getCurrentUserId() stub.
  </action>
  <verify>
    <automated>grep -c "submitEvaluate\|listUserEvaluates\|listGoodsEvaluates" zlt-business/mall-center/src/main/java/com/central/mall/controller/EvaluateController.java</automated>
  </verify>
  <done>EvaluateController has all 3 endpoints. POST / validates star/content/images. GET endpoints support pagination.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {小程序 -> API} | Untrusted user input for evaluation content and ratings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | User evaluates another user's order | mitigate | Validate order.userId == currentUserId in submitEvaluate |
| T-04-02 | Tampering | Submitting HTML/script in content field | mitigate | Content stored as text; validate max 500 chars; frontend escapes on display |
| T-04-03 | Information | Uploading non-image files | mitigate | file-center validates MIME type (image/jpeg, image/png, image/webp) |
| T-04-04 | Denial | Duplicate evaluation per orderItem | mitigate | Unique index on orderItemId in mall_evaluate; service checks before insert |
| T-04-05 | Denial | Evaluation spam on goods | accept | Pagination limits query load; no rate limit for v1 |
</threat_model>

<verification>
- MallEvaluate entity has orderItemId with unique constraint
- EvaluateServiceImpl checks order.status == 4 before evaluation
- EvaluateServiceImpl checks orderItemId uniqueness before insert
- EvaluateController POST / validates star (1-5), content (max 500), images (max 9)
- listUserEvaluates and listGoodsEvaluates return paginated IPage
</verification>

<success_criteria>
- User can submit evaluation only for completed orders (status=4)
- Duplicate evaluation per order item is rejected (unique constraint)
- User can view their evaluation history paginated
- Goods detail can show evaluation list paginated
- star (1-5), content (max 500 chars), images (JSON array max 9 URLs) stored correctly
- MallEvaluateMapper extends BaseMapper
</success_criteria>

<output>
After completion, create `.planning/phases/04-用户侧评价与交互/04-01-SUMMARY.md`
</output>
