---
phase: "05"
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallRefund.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallRefundItem.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallRefundMapper.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/mapper/MallRefundItemMapper.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/model/dto/RefundApplyDTO.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/service/IRefundService.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/service/impl/RefundServiceImpl.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/controller/RefundController.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/service/IStockService.java"
  - "zlt-business/mall-center/src/main/java/com/central/mall/service/impl/StockServiceImpl.java"
autonomous: false
requirements:
  - "REFUND-01"
  - "REFUND-02"
  - "REFUND-03"
  - "REFUND-04"
  - "REFUND-05"
  - "REFUND-06"
  - "REFUND-07"
  - "REFUND-08"
  - "REFUND-09"
  - "REFUND-10"
---

<objective>
实现退款模块：用户申请退款 → 管理员审核 → 微信退款API → 库存回增
</objective>

<read_first>
- zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallOrder.java
- zlt-business/mall-center/src/main/java/com/central/mall/service/IStockService.java
- zlt-business/mall-center/src/main/java/com/central/mall/service/impl/StockServiceImpl.java
- zlt-business/mall-center/src/main/java/com/central/mall/service/IPayService.java
</read_first>

<tasks>

<task>
<read_first>
- zlt-business/mall-center/src/main/java/com/central/mall/model/entity/MallOrder.java
</read_first>
<action>
在 MallOrder.java 中添加新的订单状态常量：

```java
/** 退款中 */
public static final Integer STATUS_REFUNDING = 6;
/** 已退款 */
public static final Integer STATUS_REFUNDED = 7;
```

更新 `STATUS_MAP` 和 `getStatusName()` 方法。
</action>
<acceptance_criteria>
- MallOrder.java 包含 STATUS_REFUNDING=6 和 STATUS_REFUNDED=7 常量
- STATUS_MAP 包含这两个新状态的映射
</acceptance_criteria>
</task>

<task>
<read_first>
- zlt-business/mall-center/src/main/java/com/central/common/model/SuperEntity.java
</read_first>
<action>
创建 `MallRefund.java` 实体类（放在 `com.central.mall.model.entity`）：

```java
@TableName("mall_refund")
public class MallRefund extends SuperEntity {
    private Long orderId;
    private String orderNo;
    private Long userId;
    private String refundNo;        // 唯一键，用于幂等
    private Integer refundType;     // 1=仅退款, 2=退货退款
    private BigDecimal refundAmount;
    private String reason;
    private String evidenceImages;   // JSON array of image URLs
    private Integer status;         // 1=待审核,2=审核通过,3=审核拒绝,4=退款中,5=已完成,6=已关闭
    private Long adminId;
    private String adminRemark;
    private String wechatRefundNo;
    private LocalDateTime refundTime;
    
    // getter/setter
}
```

创建 `MallRefundItem.java` 实体类：

```java
@TableName("mall_refund_item")
public class MallRefundItem extends SuperEntity {
    private Long refundId;
    private Long orderItemId;
    private Long skuId;
    private Long goodsId;
    private Integer quantity;
    private BigDecimal refundAmount;
    
    // getter/setter
}
```

对应的 Mapper 接口：
- `MallRefundMapper.java` 继承 `BaseMapper<MallRefund>`
- `MallRefundItemMapper.java` 继承 `BaseMapper<MallRefundItem>`
</action>
<acceptance_criteria>
- MallRefund.java 包含所有必需字段（orderId, refundNo, refundType, refundAmount, status等）
- MallRefundItem.java 包含所有必需字段（refundId, orderItemId, skuId, goodsId, quantity等）
- 两个 Mapper 接口存在并继承 BaseMapper
</acceptance_criteria>
</task>

<task>
<read_first>
- zlt-business/mall-center/src/main/java/com/central/mall/model/dto/RefundApplyDTO.java
</read_first>
<action>
创建退款申请 DTO：

```java
@Data
public class RefundApplyDTO {
    @NotNull(message = "订单ID不能为空")
    private Long orderId;
    
    @NotNull(message = "退款类型不能为空")
    private Integer refundType;  // 1=仅退款, 2=退货退款
    
    @NotNull(message = "退款金额不能为空")
    @DecimalMin(value = "0.01", message = "退款金额最小0.01元")
    private BigDecimal refundAmount;
    
    @Size(max = 500, message = "退款原因最多500字")
    private String reason;
    
    private String evidenceImages;  // JSON array: ["url1","url2"]
}
```
</action>
<acceptance_criteria>
- RefundApplyDTO.java 存在且包含 orderId, refundType, refundAmount, reason 字段
</acceptance_criteria>
</task>

<task>
<read_first>
- zlt-business/mall-center/src/main/java/com/central/mall/service/IRefundService.java
</read_first>
<action>
创建 `RefundState` 枚举类实现状态机转换校验：

```java
public enum RefundState {
    PENDING_AUDIT(1, "待审核"),
    AUDIT_PASSED(2, "审核通过"),
    AUDIT_REJECTED(3, "审核拒绝"),
    REFUNDING(4, "退款中"),
    REFUND_SUCCESS(5, "已完成"),
    REFUND_FAILED(6, "已关闭");

    private final Integer code;
    private final String desc;

    // canTransition(from, to) 方法：判断是否可以从 from 转换到 to
    // PENDING_AUDIT -> AUDIT_PASSED (approve) -> REFUNDING -> REFUND_SUCCESS
    // PENDING_AUDIT -> AUDIT_REJECTED (reject)
    // PENDING_AUDIT -> CANCELLED (cancel by user)
}
```

创建 `IRefundService.java` 接口：

```java
public interface IRefundService {
    // 用户端
    Result<?> applyRefund(RefundApplyDTO dto, Long userId);
    Result<?> cancelRefund(Long refundId, Long userId);
    Result<?> getUserRefundList(Long userId, PageDTO pageDTO);
    Result<?> getRefundDetail(Long refundId, Long userId);
    
    // 管理端
    Result<?> getAdminRefundList(RefundQueryDTO queryDTO);
    Result<?> approveRefund(Long refundId, Long adminId, String remark);
    Result<?> rejectRefund(Long refundId, Long adminId, String remark);
}
```

实现 `RefundServiceImpl.java`，包含：
- 幂等检查：使用 `refund_no` 唯一键 + Redis 分布式锁
- 退款状态机：调用 `RefundState.canTransition()` 校验状态转换
- 管理员审核：调用微信退款API（`processRefund` in `IPayService`）
- 库存回增：调用 `stockService.restoreStockOnRefund(orderId)`
</action>
<acceptance_criteria>
- RefundState.java 存在并包含完整的状态转换校验
- IRefundService.java 包含所有必需的接口方法
- RefundServiceImpl.java 实现类存在
</acceptance_criteria>
</task>

<task>
<read_first>
- zlt-business/mall-center/src/main/java/com/central/mall/service/IPayService.java
- zlt-business/mall-center/src/main/java/com/central/mall/service/impl/PayServiceImpl.java
</read_first>
<action>
在 `IPayService.java` 中添加退款方法声明：

```java
/**
 * 微信支付退款
 * @param orderId 订单ID
 * @param refundAmount 退款金额
 * @param refundNo 退款单号（幂等键）
 * @return 微信退款流水号
 */
Result<String> processRefund(Long orderId, BigDecimal refundAmount, String refundNo);
```

在 `PayServiceImpl.java` 中实现退款逻辑：
- 调用微信支付 v3 退款API：`POST https://api.mch.weixin.qq.com/v3/refund/domestic/refunds`
- 使用唯一 `refund_no` 保证幂等性
- 使用 `RedissonClient.getLock("refund:order:" + orderId)` 分布式锁防止并发
</action>
<acceptance_criteria>
- IPayService.java 包含 `processRefund` 方法声明
- PayServiceImpl.java 实现微信退款API调用逻辑
</acceptance_criteria>
</task>

<task>
<read_first>
- zlt-business/mall-center/src/main/java/com/central/mall/service/IStockService.java
- zlt-business/mall-center/src/main/java/com/central/mall/service/impl/StockServiceImpl.java
</read_first>
<action>
在 `IStockService.java` 中添加库存回增方法声明：

```java
/**
 * 退款时回增库存
 * @param orderId 订单ID
 */
void restoreStockOnRefund(Long orderId);
```

在 `StockServiceImpl.java` 中实现 `restoreStockOnRefund`：
- 查询订单关联的商品明细
- 对每个商品执行 `restoreStock(skuId, quantity)` 恢复真实库存
- 记录操作日志 `MallStockLog`，设置 `operationType = 5`（退款回增）
</action>
<acceptance_criteria>
- IStockService.java 包含 `restoreStockOnRefund` 方法声明
- StockServiceImpl.java 实现库存回增逻辑，包含 operationType=5 的日志记录
</acceptance_criteria>
</task>

<task>
<read_first>
- zlt-business/mall-center/src/main/java/com/central/mall/controller/RefundController.java
</read_first>
<action>
创建 `RefundController.java`：

```java
@RestController
@RequestMapping("/refund")
public class RefundController {
    @Autowired
    private IRefundService refundService;
    
    // 用户端
    @PostMapping
    public Result<?> applyRefund(@RequestBody RefundApplyDTO dto, @LoginUser Long userId);
    
    @PostMapping("/{id}/cancel")
    public Result<?> cancelRefund(@PathVariable Long id, @LoginUser Long userId);
    
    @GetMapping("/list")
    public Result<?> getUserRefundList(@LoginUser Long userId, PageDTO pageDTO);
    
    @GetMapping("/{id}")
    public Result<?> getRefundDetail(@PathVariable Long id, @LoginUser Long userId);
    
    // 管理端
    @GetMapping("/admin/list")
    @SaRouteCheck(role = "admin")
    public Result<?> getAdminRefundList(RefundQueryDTO queryDTO);
    
    @PostMapping("/admin/{id}/approve")
    @SaRouteCheck(role = "admin")
    public Result<?> approveRefund(@PathVariable Long id, @LoginUser Long adminId, @RequestBody RemarkDTO remark);
    
    @PostMapping("/admin/{id}/reject")
    @SaRouteCheck(role = "admin")
    public Result<?> rejectRefund(@PathVariable Long id, @LoginUser Long adminId, @RequestBody RemarkDTO remark);
}
```
</action>
<acceptance_criteria>
- RefundController.java 存在且包含所有用户端和管理端接口
- @SaRouteCheck(role = "admin") 注解存在于管理端接口
</acceptance_criteria>
</task>

<task>
<action>
执行数据库迁移脚本（创建 `mall_refund` 和 `mall_refund_item` 表）：

```sql
CREATE TABLE `mall_refund` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `tenant_id` VARCHAR(32),
    `order_id` BIGINT NOT NULL,
    `order_no` VARCHAR(64) NOT NULL,
    `user_id` BIGINT NOT NULL,
    `refund_no` VARCHAR(64) NOT NULL UNIQUE COMMENT 'Idempotency key',
    `refund_type` TINYINT DEFAULT 1 COMMENT '1=仅退款, 2=退货退款',
    `refund_amount` DECIMAL(10,2) NOT NULL,
    `reason` VARCHAR(500),
    `evidence_images` TEXT,
    `status` TINYINT DEFAULT 1 COMMENT '1=待审核,2=审核通过,3=审核拒绝,4=退款中,5=已完成,6=已关闭',
    `admin_id` BIGINT,
    `admin_remark` VARCHAR(255),
    `wechat_refund_no` VARCHAR(64),
    `refund_time` DATETIME,
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    KEY `idx_order_id` (`order_id`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_status` (`status`)
);

CREATE TABLE `mall_refund_item` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `refund_id` BIGINT NOT NULL,
    `order_item_id` BIGINT NOT NULL,
    `sku_id` BIGINT NOT NULL,
    `goods_id` BIGINT NOT NULL,
    `quantity` INT NOT NULL,
    `refund_amount` DECIMAL(10,2) NOT NULL,
    KEY `idx_refund_id` (`refund_id`)
);
```
</action>
<acceptance_criteria>
- mall_refund 表创建成功，包含所有字段
- mall_refund_item 表创建成功，包含所有字段
</acceptance_criteria>
</task>

</tasks>

<verification>
1. 编译通过：`mvn compile -pl zlt-business/mall-center -am -q`
2. 单元测试：所有 IRefundService 方法存在且可编译
</verification>

<success_criteria>
- 退款申请流程完整（用户申请 → 管理员审核 → 微信退款 → 库存回增）
- 退款状态机正确（待审核→审核通过→退款中→已完成/失败）
- 幂等性保证（refund_no 唯一键 + Redis 分布式锁）
</success_criteria>
